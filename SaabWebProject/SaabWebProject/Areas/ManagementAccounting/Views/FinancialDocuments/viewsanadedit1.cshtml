

@using SaabWebProject.Models.DomainModels;

@{
    ViewBag.Title = "تعریف اسناد";
    Layout = null;
    SaabEntities db = new SaabEntities();

}
@model IEnumerable<SaabWebProject.Models.DomainModels.dbAsnadcratasnadrelatin>
@{
    int id = 0;
    foreach (var it in Model)
    {
        id = (int)it.FK_asliasnad;
    }
    var fin = db.dbAsnadcratasnad.Where(p => p.ID == id).FirstOrDefault();
    var idd = 0;
}
<link href="~/Assets/plugins/select-picker/dist/picker.min.css" rel="stylesheet" />

<script src="~/Assets/js/jalalidatepicker.min.js"></script>
<link href="~/Assets/css/jalalidatepicker.min.css" rel="stylesheet" />
<script src="~/Assets/js/jalaali.min.js"></script>
<link href="~/MyStyles/jalalidatepicker.css" rel="stylesheet" />

<link href="~/MyStyles/MyStyle.css" rel="stylesheet" />
<link href="~/MyStyles/Tablestyle.css" rel="stylesheet" />

<link href="~/MyStyles/MyStyle.css" rel="stylesheet" />
<div class="row" style="margin-top:8px">


    <div class="col-xl-5">
        <label class="form-label"> فروشنده </label>
        <select class="form-select " id="naemshr">
            <option value="@fin.dbAsnadcratsher.ID" selected>@fin.dbAsnadcratsher.nameshr"</option>
            @Html.Action("viewfortariasnalviewname", "FinancialDocuments", new { area = "ManagementAccounting" })

        </select>

        @*<input class="form-control" id="naemshr" value="@fin.dbAsnadcratsher.nameshr" />*@

        @*<select class="form-select " id="naemshr">
                <option>انتخاب کنید</option>
                @Html.Action("viewfortariasnalviewname", "FinancialDocuments", new { area = "ManagementAccounting" })
            </select>*@
    </div>


    @*<div class="col-xl-3">
            <label class="form-label"> نام مکان </label>
            <select class="form-select ewds " id="naemshr32">
                <option>انتخاب کنید</option>
                @Html.Action("viewfortariasnalviewname", "FinancialDocuments", new { area = "ManagementAccounting" })
            </select>
        </div>*@
    <div class="col-xl-1">
        <label class="form-label">  شماره فاکتور</label>
        <input class="form-control" id="codsamanehmoadyan" value="@fin.codsamanehmoadyan" />
    </div>
    @*<div class="col-xl-2">
            <label class="form-label">  کد سامانه مودیان </label>
            <input class="form-control" id="endfalse" />
        </div>*@

    <div class="col-xl-1">
        <label class="form-label"> تاریخ فاکتور </label>
        <div class="col-xl-12">
            @*------------------------------------------------[1402/10/14]-|KH|-*@
            @if (fin.data != null)
            {
                <input data-jdp jdp-btn-close data-jdp-miladi-input="datapick" id="datapickee" class="form-control pdate" name="datepickerFrom_shw1">
                <input id="datapick" value="@fin.data.Value.ToString("MM/dd/yyyy")" type="hidden" />
            }
            else
            {
                <input data-jdp jdp-btn-close data-jdp-miladi-input="datapick" id="datapickee" class="form-control pdate" name="datepickerFrom_shw1">
                <input id="datapick" type="hidden" />
            }
            @*<input data-jdp jdp-btn-close data-jdp-miladi-input="datepickerFromEn_Eq12" id="datepickerFrom_Eq12" class="form-control">
                <input id="datepickerFromEn_Eq12" type="hidden" />*@
            @*-------------------------------------------------------------|KH|-*@
        </div>
    </div>
    <div class="col-xl-5">
        <label class="form-label"> شرح</label>
        <input class="form-control" id="sharhasli" value="@fin.titel" />
    </div>





    @*<div class="col-xl-2">
            <label class="form-label"> درجه اهمیت </label>
            <select class="form-select my_Select" id="import">
                <option value="0">انتخاب کنید..</option>
                <option value="1">مهم </option>
                <option value="2">عادی </option>
                <option value="3">بسیار مهم </option>

            </select>
        </div>
        <div class="col-xl-6">
            <label class="form-label">  شرح  </label>
            <textarea class="form-control" id="Title2"></textarea>
        </div>*@


</div>
<hr />
@foreach (var it in Model.Where(p => p.count != null).ToList())
{


    <div class="row" id="row_@it.ID" style="margin-top:8px">
        <div class="col-xl-1" style="display:none">
            <label class="form-label"> کد </label>
            @if (it.FK_nameshr != null)
            {
                <input class="form-control installmentAmountInputManually3" id="cod_@it.ID" @*oninput="formatAndCalculate(${nodesLength})"*@ value="@db.dbAsnadcratname.Where(p => p.ID == it.FK_nameshr).Select(s => s.codmahsol).FirstOrDefault()" />

            }
            else
            {
                <input class="form-control installmentAmountInputManually3" id="cod_@it.ID" @*oninput="formatAndCalculate(${nodesLength})"*@ />

            }

        </div>
        <div class="col-xl-3">
            <label class="form-label"> عنوان کالا </label>
            <select class="form-select installmentAmountInputManually" id="creditline_@it.ID" onchange="updateIntervalField(@it.ID)">

                @if (it.FK_nameshr != null)
                {
                    <option value="@it.FK_nameshr" selected> @db.dbAsnadcratname.Where(p => p.ID == it.FK_nameshr).Select(s => s.name).FirstOrDefault()"</option>
                    @Html.Action("viewfortariasnalvasil", "FinancialDocuments", new { area = "ManagementAccounting" })

                }
                else
                {
                    <option>انتخاب کنید</option>
                    @Html.Action("viewfortariasnalvasil", "FinancialDocuments", new { area = "ManagementAccounting" })
                }
            </select>

            @*<select class="form-select installmentAmountInputManually" id="creditline_${nodesLength}" onchange="updateIntervalField(${nodesLength})">
                    <option>انتخاب کنید</option>
                    @Html.Action("viewfortariasnalvasil", "FinancialDocuments", new { area = "ManagementAccounting" })
                </select>*@
        </div>

        <div class="col-xl-1">
            <label class="form-label"> مقدار </label>
            <input class="form-control installmentAmountInputManually3" id="bestankar_@it.ID" oninput="formatAndCalculate(@it.ID)" value="@String.Format("{0:N0}", it.count)" />
        </div>
        <div class="col-xl-1">
            <label class="form-label"> واحد  </label>
            <input class="form-control installmentAmountInputManually3" id="bedehkar34_@it.ID" oninput="formatAndCalculate(@it.ID)" disabled value="" />
        </div>
        <div class="col-xl-1">
            <label class="form-label"> مبلغ واحد  </label>
            <input class="form-control installmentAmountInputManually3" id="bedehkar3_@it.ID" oninput="formatAndCalculate(@it.ID)" value="@String.Format("{0:N0}",it.vahd)" />
        </div>

        <div class="col-xl-1">
            <label class="form-label">مبلغ اولیه </label>
            <input class="form-control installmentAmountInputManually2" id="bedehkar_@it.ID" disabled value="@String.Format("{0:N0}",it.megdar)" />
        </div>
        <div class="col-xl-1">
            <label class="form-label"> مبلغ تخفیف  </label>
            <input class="form-control installmentAmountInputManually3" id="bedehkar30_@it.ID" oninput="formatAndCalculate(@it.ID)" value="@String.Format("{0:N0}",it.taghfif)" />
        </div>
        <div class="col-xl-1">
            <label class="form-label"> مبلغ با تخفیف  </label>
            <input class="form-control installmentAmountInputManually3" id="bedehkar31_@it.ID" oninput="formatAndCalculate(@it.ID)" disabled value="@String.Format("{0:N0}",it.bytaghfif)" />
        </div>

        <div class="col-xl-1">
            <label class="form-label"> مبلغ ارزش افزوده  </label>
            <input class="form-control installmentAmountInputManually3" id="bedehkar32_@it.ID" oninput="formatAndCalculate(@it.ID)" value="@String.Format("{0:N0}",it.arzish)" />
        </div>

        <div class="col-xl-1">
            <label class="form-label">مبلغ نهایی </label>
            <input class="form-control installmentAmountInputManually3" id="bedehkar33_@it.ID" oninput="formatAndCalculate(@it.ID)" disabled value="@String.Format("{0:N0}",it.nahaei)" />
        </div>

        <div class="col-xl-1">
            <button type="button" class="close btn form-control buttonGeneralMins" aria-label="Close" onclick="removeRow(@it.ID)">
                <i class="fa fa-multiply fa-fw VecGeneral"></i>
            </button>
        </div>

    </div>
    <script src="~/Assets/plugins/select-picker/dist/picker.min.js"></script>

    <script>
        $('#creditline_' + @it.ID).picker({
            search: true
        });
    </script>
    idd = it.ID;
}
<hr />

@foreach (var it in Model.Where(p => p.count == null && p.taghfif != null).ToList())
{
    <div class="row" id="row_@it.ID" style="margin-top:8px">
        <div class="col-xl-1" style="display:none">
            <label class="form-label"> کد </label>
            @if (it.FK_nameshr != null)
            {
                <input class="form-control installmentAmountInputManually3" id="cod_@it.ID" @*oninput="formatAndCalculate(${nodesLength})"*@ value="@db.dbAsnadcratname.Where(p => p.ID == it.FK_nameshr).Select(s => s.codmahsol).FirstOrDefault()" />

            }
            else
            {
                <input class="form-control installmentAmountInputManually3" id="cod_@it.ID" @*oninput="formatAndCalculate(${nodesLength})"*@ />

            }

        </div>

        <div class="col-xl-3">

            <label class="form-label"> عنوان کالا </label>
            <select class="form-select installmentAmountInputManually345677323237" id="creditline2wt345_@it.ID" onchange="updateIntervalField(@it.ID)">

                @if (it.FK_nameshr != null)
                {
                    <option value="@it.FK_nameshr" selected> @db.dbAsnadcratname.Where(p => p.ID == it.FK_nameshr).Select(s => s.name).FirstOrDefault()"</option>
                    @Html.Action("viewfortariasnalvasil", "FinancialDocuments", new { area = "ManagementAccounting" })

                }
                else
                {
                    <option>انتخاب کنید</option>
                    @Html.Action("viewfortariasnalvasil", "FinancialDocuments", new { area = "ManagementAccounting" })
                }
            </select>
        </div>

        <div class="col-xl-1">
            <span> تخفیف</span>

        </div>


        <div class="col-xl-6">
            <label class="form-label"> شرح تخفیف  </label>
            <input class="form-control " id="bedehkar300000_@it.ID" value="@it.sharhtaghfif" />
        </div>


        <div class="col-xl-1">
            <label class="form-label"> مبلغ تخفیف  </label>
            <input class="form-control installmentAmountInputManually3000" id="bedehkar3045556_@it.ID" oninput="calculateTotal456(@it.ID)" value="@String.Format("{0:N0}",it.taghfif)" />
        </div>
        @*<div class="col-xl-3">
        </div>*@
        <div class="col-xl-1">
            <button type="button" class="close btn form-control buttonGeneralMins" aria-label="Close" onclick="removeRow(@it.ID)">
                <i class="fa fa-multiply fa-fw VecGeneral"></i>
            </button>
        </div>
    </div>
    <script src="~/Assets/plugins/select-picker/dist/picker.min.js"></script>

    <script>
        $('#creditline2wt345_' + @it.ID).picker({
        search: true
    });
    </script>
    idd = it.ID;

}
<hr />

@foreach (var it in Model.Where(p => p.count == null && p.arzish != null).ToList())
{
    <div class="row" id="row_@it.ID" style="margin-top:8px">
        <div class="col-xl-3">

            <label class="form-label"> عنوان کالا </label>
            <select class="form-select installmentAmountInputManually3456777" id="creditline2w345_@it.ID" onchange="updateIntervalField(@it.ID)">

                @if (it.FK_nameshr != null)
                {
                    <option value="@it.FK_nameshr" selected> @db.dbAsnadcratname.Where(p => p.ID == it.FK_nameshr).Select(s => s.name).FirstOrDefault()"</option>
                    @Html.Action("viewfortariasnalvasil", "FinancialDocuments", new { area = "ManagementAccounting" })

                }
                else
                {
                    <option>انتخاب کنید</option>
                    @Html.Action("viewfortariasnalvasil", "FinancialDocuments", new { area = "ManagementAccounting" })
                }
            </select>
        </div>

        <div class="col-xl-1">
            <span> ارزش افزوده</span>

        </div>
        <div class="col-xl-6">
            <label class="form-label"> شرح ارزش افزوده  </label>
            <input class="form-control " id="bedehkar300000000_@it.ID" value="@it.sharharzesh" />
        </div>
        @*<div class="col-xl-1">
        </div>
        <div class="col-xl-1">
        </div>*@




        <div class="col-xl-1">
            <label class="form-label"> مبلغ ارزش افزوده  </label>
            <input class="form-control installmentAmountInputManually302" id="bedehkar323577865_@it.ID" oninput="calculateTotal456(@it.ID)" value="@String.Format("{0:N0}",it.arzish)" />
        </div>


        <div class="col-xl-1">
            <button type="button" class="close btn form-control buttonGeneralMins" aria-label="Close" onclick="removeRow(@it.ID)">
                <i class="fa fa-multiply fa-fw VecGeneral"></i>
            </button>
        </div>
    </div>
    <script src="~/Assets/plugins/select-picker/dist/picker.min.js"></script>

    <script>
        $('#creditline2w345_' + @it.ID).picker({
        search: true
    });
    </script>
    idd = it.ID;

}

<hr />
<div class="row">
    <div id="SectionInstallmentInputsedit"></div>
</div>
<hr />
<div class="row">
    <div class="row">
        <div class="col-xl-2">
            <span>جمع کل</span>
        </div>
        <div class="col-xl-4">
        </div>
        <div class="col-xl-1">
            @*<label class="form-label">مبلغ اولیه </label>*@
            <input class="form-control" id="sumkol1" disabled />
        </div>
        <div class="col-xl-1">
            @*<label class="form-label">تخفیف </label>*@
            <input class="form-control" id="sumkol2" disabled />
        </div>
        <div class="col-xl-1">
            @*<label class="form-label">مبلغ با تخفیف </label>*@
            <input class="form-control" id="sumkol3" disabled />
        </div>
        <div class="col-xl-1">
            @*<label class="form-label">ارزش افزوده </label>*@
            <input class="form-control" id="sumkol4" disabled />
        </div>
        <div class="col-xl-2">
            @*<label class="form-label">مبلغ نهایی </label>*@
            <input class="form-control" id="sumkol" disabled value="@String.Format("{0:N0}",fin.sumstring)" />
        </div>
    </div>
    <hr />


</div>
<hr />

<div class="row">


    <div class="col-xl-1">
        <div class="form-group mb-3">
            <button id="btnPlusInput" onclick="plusInputInstallmentManuallyedit(this)" class="btn form-control buttonGeneralPlus">
                <i class="fa fa-plus fa-fw me-1  VecGeneral"></i>
            </button>
        </div>
    </div>
    <div class="col-xl-1">
        <div class="form-group mb-3">
            <button id="btnPlusInput" onclick="plusInputInstallmentManuallyedit1(this)" class="btn form-control buttonGeneralPlus">
                تخفیف
            </button>
        </div>
    </div>
    <div class="col-xl-1">
        <div class="form-group mb-3">
            <button id="btnPlusInput" onclick="plusInputInstallmentManuallyedit2(this)" class="btn form-control buttonGeneralPlus">
                ارزش افزوده
            </button>
        </div>
    </div>


    <div class="col-xl-2">
        <label class="form-label"> درج شماره سند </label>
        @*<select class="form-select " id="sanad">
                <option>انتخاب کنید</option>
                @for (int I = 1; I <= 10; I++)
                {
                    <option value="@I">@I</option>

                }

            </select>*@
        <input class="form-control" id="sanad" value="@fin.sanadmalyy" />
    </div>
</div>
<hr />

<!--<div class="row">
<div class="col-xl-2">
    <label class="form-label">  شماره سند مالی </label>
    <input class="form-control" id="sanad" value="@fin.sanadmalyy" />-->
@*<input class="form-control" id="sanad" />*@
<!--</div>
</div>-->
<div class="row">
    <div class="col-xl-3">
        <label class="form-label"> نام کاربر حقیقی </label>
        <select class="form-select " id="namefard">
            @if (fin.FK_usr != null)
            {
                <option value="@fin.tbUsers.usr_ID" selected>@fin.tbUsers.FullName</option>
                @Html.Action("usr", "FinancialDocuments", new { area = "ManagementAccounting" })
            }
            else
            {
                <option>انتخاب کنید</option>
                @Html.Action("usr", "FinancialDocuments", new { area = "ManagementAccounting" })
            }


        </select>
        @*@if (fin.FK_usr != null)
            {
                <input class="form-control" id="namefard" value="@fin.tbUsers.FullName" />

            }
            else
            {
                <input class="form-control" id="namefard" />

            }*@
        @*<select class="form-select " id="namefard">

                <option>انتخاب کنید</option>
                @Html.Action("usr", "FinancialDocuments", new { area = "ManagementAccounting" })

            </select>*@
    </div>
    <div class="col-xl-2">
        <label class="form-label">مبلغ پرداخت</label>

        <input class="form-control" id="pardaght" oninput="formatNumberInput222(this)" value="@fin.pardaght" />
    </div>

    <div class="col-xl-2">
        <label class="form-label"> تاریخ پرداخت </label>
        <div class="col-xl-12">
            @*------------------------------------------------[1402/10/14]-|KH|-*@
            @if (fin.datausr != null)
            {
                <input data-jdp jdp-btn-close data-jdp-miladi-input="datapick2" id="datapickee2" class="form-control pdate" name="datepickerFrom_shw1">
                <input id="datapick2" value="@fin.datausr.Value.ToString("MM/dd/yyyy")" type="hidden" />
            }

            else
            {
                <input data-jdp jdp-btn-close data-jdp-miladi-input="datapick2" id="datapickee2" class="form-control pdate" name="datepickerFrom_shw1">
                <input id="datapick2" @*value="null"*@ type="hidden" />
            }

            @*-------------------------------------------------------------|KH|-*@
        </div>
    </div>
    <div class="col-xl-2">
        <label class="form-label">  شماره پیگیری </label>
        <input class="form-control" id="shomarehpardaght" @*value="@fin.sh"*@ />
    </div>        <div class="col-xl-1">
        <button class="btn form-control buttonGeneral uploadSummery2">
            <i class="bi bi-upload fa-fw me-1 VecGeneral" style="width:11px"></i>
            <span class="bGeneral-text">بارگذاری پیوست</span>
        </button>
        <input id="summery_file2" style="display: none;" type="file" class="form-control" value="" />
    </div>


    <div class="col-xl-1">
        <button class="btn form-control buttonsabt" id="btnsabtavaleh403">
            <i class="bi bi-clipboard-check fa-fw me-1 VecGeneral"></i>
            <span class="bSabt-text">ثبت</span>
        </button>
    </div>
    <div class="col-xl-1">
        <button class="btn form-control buttonsabt" id="btnsabtnahei1403">
            <i class="bi bi-clipboard-check fa-fw me-1 VecGeneral"></i>
            <span class="bSabt-text"> ثبت نهایی</span>
        </button>
    </div>
</div>
<script src="~/Scripts/jquery-ui.js"></script>
<script src="~/Assets/js/jquery3.6.0.js"></script>
<script src="~/Assets/plugins/select-picker/dist/picker.min.js"></script>
<script>
    var fd = new FormData();

    $(".uploadSummery2").click(function () {
        $("#summery_file2").click();
    });

    $("#summery_file2").change(function (e) {
        $.each(e.target.files, function (index, file) {
            fd.append("files", file);
        });
    });
</script>
<script>
    $('#naemshr').picker({
        search: true
    });
    $('#namefard').picker({
        search: true
    });
</script>
<script>

    var sumpol = 0; // Declare outside the function to maintain its value
    function updateIntervalField(rowId) {
        // Find the selected option in the dropdown
        var selectedOption = $(`#creditline_${rowId} option:selected`);

        // Get the data-interval attribute from the selected option
        var intervalValue = selectedOption.data("interval");
        var intervalValue2 = selectedOption.data("delay");

        console.log(intervalValue);
        // Set the intervalValue in the corresponding input field
        $(`#bedehkar34_${rowId}`).val(intervalValue);
        $(`#cod_${rowId}`).val(intervalValue2);
    }
     function formatCurrency(input) {
            // حذف همه کاراکترهای غیر از اعداد و "." (جداکننده اعشاری)
            let value = input.value.replace(/[^\d.]/g, '');

        // تبدیل مقدار به عدد اعشاری
        let number = parseFloat(value);

        // اگر عدد معتبر باشد، آن را به صورت سه رقمی جداکنده کنید
        if (!isNaN(number)) {
            let formattedValue = number.toLocaleString('en-US', {minimumFractionDigits: 0, maximumFractionDigits: 0 });
        // قرار دادن مقدار فرمت شده در ورودی
        input.value = formattedValue;
        }
    }
    jalaliDatepicker.startWatch({
        time: false,
        days: ["شنبه", "1شنبه", "2شنبه", "3شنبه", "4شنبه", "5شنبه", "جمعه"],
        persianDigits: true,
        autoClose: true,
        //autocomplete: true,
        showCloseBtn: true,
        showEmptyBtn: false,
        showCloseBtn: true,
        zIndex: 100000,
    });
    function reinitializeDatepickers() {
        var inputs = document.querySelectorAll("[data-jdp-miladi-input]");
        console.log(inputs.length);

        for (var i = 0; i < inputs.length; i++) {
            inputs[i].addEventListener("jdp:change", function (e) {
                var miladiInput = document.getElementById(this.getAttribute("data-jdp-miladi-input"));
                var date = this.value.replace(' ', '').slice(0, 10).split('/');
                date = jalaali.toGregorian(parseInt(date[0]), parseInt(date[1]), parseInt(date[2]));

                if (!this.value) {
                    date = "";
                } else if (this.value.replace(' ', '').length < 11) {
                    date = date.gy + '-' + date.gm + '-' + date.gd;
                } else {
                    date = date.gy + '-' + date.gm + '-' + date.gd + ' ' + this.value.replace(' ', '').slice(10, 30);
                }
                miladiInput.value = date;
            });
        }
    }

    //-----------------------------------------------------------------
    var inputs = document.querySelectorAll("[data-jdp-miladi-input]");
    for (var i = 0; i < inputs.length; i++) {
        console.log(inputs.length);

        inputs[i].addEventListener("jdp:change", function (e) {
            var miladiInput = document.getElementById(this.getAttribute("data-jdp-miladi-input"));
            var date = this.value.replace(' ', '').slice(0, 10).split('/');
            date = jalaali.toGregorian(parseInt(date[0]), parseInt(date[1]), parseInt(date[2]),)
            // If it was empty
            if (!this.value) {
                date = "";
            }
            // If it was only history
            else if (this.value.replace(' ', '').length < 11) {
                date = date.gy + '-' + date.gm + '-' + date.gd
            }
            // If it was the date and time
            else {
                date = date.gy + '-' + date.gm + '-' + date.gd + ' ' + this.value.replace(' ', '').slice(10, 30)
            }
            miladiInput.value = date
        });
    }
    var valueD
    var date

    // convert miladi to jalali
    date = document.getElementById("datapick").value.replace(" ", "")
    console.log(date)
    valueD = new Date(date.slice(0, 10)).toLocaleDateString('fa-IR-u-nu-latn', { year: 'numeric', month: '2-digit', day: '2-digit' })
    valueD = valueD + " " + date.slice(10, 30)
    document.getElementById("datapickee").value = valueD
    date = document.getElementById("datapick2").value.replace(" ", "")
    console.log(date)
    valueD = new Date(date.slice(0, 10)).toLocaleDateString('fa-IR-u-nu-latn', { year: 'numeric', month: '2-digit', day: '2-digit' })
    valueD = valueD + " " + date.slice(10, 30)
    document.getElementById("datapickee2").value = valueD


            var nodesLength = @idd; // Declare outside the function to maintain its value
    //$(document).ready(function () {
    //    // اطمینان از اینکه پلاگین به درستی بارگذاری شده است
    //    console.log($.fn.picker);

    //    // اعمال پلاگین به تمام المنت‌های از قبل موجود
    //    $(".installmentAmountInputManually").picker({
    //        search: true
    //    });
    //});

    var tyu = 0;
    var tyyu = 0;
function plusInputInstallmentManuallyedit(e) {
    var a = 1;
    nodesLength++;
    a++;
    tyyu += tyu;
    //$('#sumkol').val(formatNumber(tyyu.toFixed(2)));

    // اضافه کردن ورودی جدید به DOM
    $("#SectionInstallmentInputsedit").append(`
        <div id="row_${nodesLength}" class="row">
        <div class="col-xl-1"style="display:none">
    <label class="form-label"> کد </label>
    <input class="form-control installmentAmountInputManually3" id="cod_${nodesLength}" /*oninput="formatAndCalculate(${nodesLength})"*/ />
</div>
            <div class="col-xl-3">
               <label class="form-label"> عنوان کالا </label>
                <select class="form-select installmentAmountInputManually" id="creditline_${nodesLength}" onchange="updateIntervalField(${nodesLength})">
                    <option>انتخاب کنید</option>
                    @Html.Action("viewfortariasnalvasil", "FinancialDocuments", new { area = "ManagementAccounting" })
                </select>
            </div>

            <div class="col-xl-1">
                <label class="form-label"> مقدار </label>
                <input class="form-control installmentAmountInputManually3 bestankar-field" id="bestankar_${nodesLength}" oninput="formatAndCalculate(${nodesLength})" />
            </div>
                        <div class="col-xl-1">
    <label class="form-label"> واحد  </label>
    <input class="form-control installmentAmountInputManually3" id="bedehkar34_${nodesLength}" oninput="formatAndCalculate(${nodesLength})" disabled/>
</div>
            <div class="col-xl-1">
                <label class="form-label"> مبلغ واحد  </label>
                <input class="form-control installmentAmountInputManually3" id="bedehkar3_${nodesLength}" oninput="formatAndCalculate(${nodesLength})" />
            </div>

            <div class="col-xl-1">
                <label class="form-label">مبلغ اولیه </label>
                <input class="form-control installmentAmountInputManually2" id="bedehkar_${nodesLength}" disabled />
            </div>
                  <div class="col-xl-1">
          <label class="form-label"> مبلغ تخفیف  </label>
          <input class="form-control installmentAmountInputManually3" id="bedehkar30_${nodesLength}" oninput="formatAndCalculate(${nodesLength})" />
      </div>
                  <div class="col-xl-1">
    <label class="form-label"> مبلغ با تخفیف  </label>
    <input class="form-control installmentAmountInputManually3" id="bedehkar31_${nodesLength}" oninput="formatAndCalculate(${nodesLength})" disabled/>
</div>

                  <div class="col-xl-1">
    <label class="form-label"> مبلغ ارزش افزوده  </label>
    <input class="form-control installmentAmountInputManually3" id="bedehkar32_${nodesLength}" oninput="formatAndCalculate(${nodesLength})" />
</div>

                  <div class="col-xl-1">
    <label class="form-label">مبلغ نهایی </label>
    <input class="form-control installmentAmountInputManually3" id="bedehkar33_${nodesLength}" oninput="formatAndCalculate(${nodesLength})" disabled/>
</div>



            <div class="col-xl-1">
                <button type="button" class="close btn form-control buttonGeneralMins" aria-label="Close" onclick="removeRow(${nodesLength})">
                    <i class="fa fa-multiply fa-fw VecGeneral"></i>
                </button>
            </div>

            <hr class="mt-0">
        </div>
    `);
    //$(`#creditline_${nodesLength}`).on('change', function () {
    //    console.log("sdakjs");
    //    updateIntervalField(nodesLength);
    //});
    //src = "~/Assets/plugins/select-picker/dist/picker.min.js"
    ////// فراخوانی پلاگین picker روی المنت جدید پس از اضافه شدن به DOM
    //$(`#bestankar_${nodesLength}`).on('focus', function () {
    //    console.log("فیلد مقدار فوکوس شد");
    //    // اینجا می‌توانید هر اتفاقی که می‌خواهید بیفتد را اضافه کنید
    //    updateIntervalField(nodesLength); // به عنوان مثال، فراخوانی یک تابع
    //});
    $('#creditline_' + nodesLength).picker({
        search: true
    });
    //$(`#creditline_${nodesLength}`).on('change', function () {
    //    console.log("sdakjs");
    //    updateIntervalField(nodesLength);
    //});

    //console.log($.fn.picker); // برای بررسی درستی بارگذاری پلاگین
    }
    $(document).on('focus', '.bestankar-field', function () {
        var rowId = $(this).attr('id').split('_')[1];  // گرفتن شماره ردیف از id
        console.log("فیلد مقدار فوکوس شد، ردیف:", rowId);
        updateIntervalField(rowId); // فراخوانی تابع به‌روزرسانی
    });
    function plusInputInstallmentManuallyedit1(e) {
        var a = 1;
        nodesLength++;
        a++;
        tyyu += tyu;
        //$('#sumkol').val(formatNumber(tyyu.toFixed(2)));

        // اضافه کردن ورودی جدید به DOM
        $("#SectionInstallmentInputsedit").append(`
        <div id="row_${nodesLength}" class="row">
                    <div class="col-xl-3">

        <label class="form-label"> عنوان کالا </label>
    <select class="form-select installmentAmountInputManually345677323237" id="creditline2wt345_${nodesLength}" onchange="updateIntervalField(${nodesLength})">
        <option>انتخاب کنید</option>
        @Html.Action("viewfortariasnalvasil", "FinancialDocuments", new { area = "ManagementAccounting" })
    </select>
</div> 

            <div class="col-xl-1">
                    <span> تخفیف</span>

            </div>
                        <div class="col-xl-6">
                     <label class="form-label"> شرح تخفیف  </label>
   <input class="form-control " id="bedehkar300000_${nodesLength}" />
</div>

                  <div class="col-xl-1">
          <label class="form-label"> مبلغ تخفیف  </label>
          <input class="form-control installmentAmountInputManually3000" id="bedehkar3045556_${nodesLength}" oninput="calculateTotal456(${nodesLength})" />
      </div>
     





            <div class="col-xl-1">
                <button type="button" class="close btn form-control buttonGeneralMins" aria-label="Close" onclick="removeRow(${nodesLength})">
                    <i class="fa fa-multiply fa-fw VecGeneral"></i>
                </button>
            </div>

            <hr class="mt-0">
        </div>
    `);
        //src = "~/Assets/plugins/select-picker/dist/picker.min.js"
        //// فراخوانی پلاگین picker روی المنت جدید پس از اضافه شدن به DOM
        //$('#creditline_' + nodesLength).picker({
        //    search: true
        //});

        //reinitializeDatepickers(); // اگر لازم است برای عناصر جدید

        //console.log($.fn.picker); // برای بررسی درستی بارگذاری پلاگین
    }

    function plusInputInstallmentManuallyedit2(e) {
        var a = 1;
        nodesLength++;
        a++;
        tyyu += tyu;
        //$('#sumkol').val(formatNumber(tyyu.toFixed(2)));

        // اضافه کردن ورودی جدید به DOM
        $("#SectionInstallmentInputsedit").append(`
        <div id="row_${nodesLength}" class="row">
                            <div class="col-xl-3">

           <label class="form-label"> عنوان کالا </label>
    <select class="form-select installmentAmountInputManually3456777" id="creditline2w345_${nodesLength}" onchange="updateIntervalField(${nodesLength})">
        <option>انتخاب کنید</option>
        @Html.Action("viewfortariasnalvasil", "FinancialDocuments", new { area = "ManagementAccounting" })
    </select>
</div>            

            <div class="col-xl-1">
                    <span> ارزش افزوده</span>

            </div>
                                      <div class="col-xl-6">
                     <label class="form-label"> شرح ارزش افزوده  </label>
   <input class="form-control " id="bedehkar300000000_${nodesLength}" />
</div>
                  <div class="col-xl-1">
    <label class="form-label"> مبلغ ارزش افزوده  </label>
    <input class="form-control installmentAmountInputManually302" id="bedehkar323577865_${nodesLength}"  oninput="calculateTotal456(${nodesLength})" />
</div>

           


            <div class="col-xl-1">
                <button type="button" class="close btn form-control buttonGeneralMins" aria-label="Close" onclick="removeRow(${nodesLength})">
                    <i class="fa fa-multiply fa-fw VecGeneral"></i>
                </button>
            </div>

            <hr class="mt-0">
        </div>
    `);
        //src = "~/Assets/plugins/select-picker/dist/picker.min.js"
        //// فراخوانی پلاگین picker روی المنت جدید پس از اضافه شدن به DOM
        ////$('#creditline_' + nodesLength).picker({
        ////    search: true
        ////});

        ////reinitializeDatepickers(); // اگر لازم است برای عناصر جدید

        //console.log($.fn.picker); // برای بررسی درستی بارگذاری پلاگین
    }
    function removeRow(rowId) {
        // Find the row element with the given ID
        var row = document.getElementById('row_' + rowId);

        // Check if the row exists
        if (row) {
            // Remove the row completely from the DOM
            row.remove();

        } else {
            console.error("Row with ID " + rowId + " does not exist.");
        }
        var tr = 0;
        var aval = 0;
        var taghfif = 0;
        var bytaghfif = 0;
        var arzesh = 0;
        $('.installmentAmountInputManually').each(function (i, obj) {
            console.log("djkjsdkl");
            let count = 0;

            if ($(this).attr("id").split("_").length == 2) {
                var obj = new Object();

                count++;
                console.log($(this).attr("id"));

                //console.log($("#select_peyman1").val());
                //console.log($("#miladi_date2").val());
                var parts = $(this).attr("id").split("_"); // Corrected line                    var inputString = $(this).val();

                //var parts = id.split("_");


                //obj.Creditorcredit = $("#bestankar").val();
                //obj.Debtorcredit = $("#bedehkar").val();

                var Year_id4 = "bedehkar33_" + parts[1];
                tr += parseFloat($('#' + Year_id4).val().replace(/,/g, '')) || 0;
                var Year_id45 = "bedehkar_" + parts[1];
                aval += parseFloat($('#' + Year_id45).val().replace(/,/g, '')) || 0;

                var Year_id456 = "bedehkar30_" + parts[1];
                taghfif += parseFloat($('#' + Year_id456).val().replace(/,/g, '')) || 0;

                var Year_id457 = "bedehkar31_" + parts[1];
                bytaghfif += parseFloat($('#' + Year_id457).val().replace(/,/g, '')) || 0;

                var Year_id458 = "bedehkar32_" + parts[1];
                arzesh += parseFloat($('#' + Year_id458).val().replace(/,/g, '')) || 0;


                console.log(tr);

                console.log(parseFloat($('#' + Year_id4).val().replace(/,/g, '')) || 0);


            }
        });
        $('.installmentAmountInputManually3000').each(function (i, obj) {
            console.log("djkjsdkl");
            let count = 0;

            if ($(this).attr("id").split("_").length == 2) {
                var obj = new Object();

                count++;
                console.log($(this).attr("id"));

                //console.log($("#select_peyman1").val());
                //console.log($("#miladi_date2").val());
                var parts = $(this).attr("id").split("_"); // Corrected line                    var inputString = $(this).val();

                //var parts = id.split("_");


                //obj.Creditorcredit = $("#bestankar").val();
                //obj.Debtorcredit = $("#bedehkar").val();
                var Year_id456 = "bedehkar3045556_" + parts[1];
                taghfif += parseFloat($('#' + Year_id456).val().replace(/,/g, '')) || 0;

                var Year_id457 = "bedehkar31_" + parts[1];
                bytaghfif += parseFloat($('#' + Year_id456).val().replace(/,/g, '')) || 0;
                tr -= parseFloat($('#' + Year_id456).val().replace(/,/g, '')) || 0;







                console.log(tr);



            }
        });
        $('.installmentAmountInputManually302').each(function (i, obj) {
            console.log("djkjsdkl");
            let count = 0;

            if ($(this).attr("id").split("_").length == 2) {
                var obj = new Object();

                count++;
                console.log($(this).attr("id"));

                //console.log($("#select_peyman1").val());
                //console.log($("#miladi_date2").val());
                var parts = $(this).attr("id").split("_"); // Corrected line                    var inputString = $(this).val();

                //var parts = id.split("_");


                //obj.Creditorcredit = $("#bestankar").val();
                //obj.Debtorcredit = $("#bedehkar").val();
                var Year_id456 = "bedehkar323577865_" + parts[1];
                arzesh += parseFloat($('#' + Year_id456).val().replace(/,/g, '')) || 0;

                tr += parseFloat($('#' + Year_id456).val().replace(/,/g, '')) || 0;







                console.log(tr);



            }
        });

        $('#sumkol1').val(formatNumber(aval.toFixed(0)));
        $('#sumkol2').val(formatNumber(taghfif.toFixed(0)));
        $('#sumkol3').val(formatNumber(bytaghfif.toFixed(0)));
        $('#sumkol4').val(formatNumber(arzesh.toFixed(0)));

        $('#sumkol').val(formatNumber(tr.toFixed(0)));
    }
    function removeRow22(rowId) {
        // Find the row element with the given ID
        var row = document.getElementById(rowId);

        // Check if the row exists
        if (row) {
            // Remove the row
            row.remove();
        } else {
            console.error("Row with ID " + rowId + " does not exist.");
        }
        // Check if the row exists
        if (row) {
            // Remove the row completely from the DOM
            row.remove();
        } else {
            console.error("Row with ID " + rowId + " does not exist.");
        }
        var tr = 0;
        var aval = 0;
        var taghfif = 0;
        var bytaghfif = 0;
        var arzesh = 0;
        $('.installmentAmountInputManually').each(function (i, obj) {
            console.log("djkjsdkl");
            let count = 0;

            if ($(this).attr("id").split("_").length == 2) {
                var obj = new Object();

                count++;
                console.log($(this).attr("id"));

                //console.log($("#select_peyman1").val());
                //console.log($("#miladi_date2").val());
                var parts = $(this).attr("id").split("_"); // Corrected line                    var inputString = $(this).val();

                //var parts = id.split("_");


                //obj.Creditorcredit = $("#bestankar").val();
                //obj.Debtorcredit = $("#bedehkar").val();

                var Year_id4 = "bedehkar33_" + parts[1];
                tr += parseFloat($('#' + Year_id4).val().replace(/,/g, '')) || 0;
                var Year_id45 = "bedehkar_" + parts[1];
                aval += parseFloat($('#' + Year_id45).val().replace(/,/g, '')) || 0;

                var Year_id456 = "bedehkar30_" + parts[1];
                taghfif += parseFloat($('#' + Year_id456).val().replace(/,/g, '')) || 0;

                var Year_id457 = "bedehkar31_" + parts[1];
                bytaghfif += parseFloat($('#' + Year_id457).val().replace(/,/g, '')) || 0;

                var Year_id458 = "bedehkar32_" + parts[1];
                arzesh += parseFloat($('#' + Year_id458).val().replace(/,/g, '')) || 0;


                console.log(tr);

                console.log(parseFloat($('#' + Year_id4).val().replace(/,/g, '')) || 0);


            }
        });
        $('.installmentAmountInputManually3000').each(function (i, obj) {
            console.log("djkjsdkl");
            let count = 0;

            if ($(this).attr("id").split("_").length == 2) {
                var obj = new Object();

                count++;
                console.log($(this).attr("id"));

                //console.log($("#select_peyman1").val());
                //console.log($("#miladi_date2").val());
                var parts = $(this).attr("id").split("_"); // Corrected line                    var inputString = $(this).val();

                //var parts = id.split("_");


                //obj.Creditorcredit = $("#bestankar").val();
                //obj.Debtorcredit = $("#bedehkar").val();
                var Year_id456 = "bedehkar3045556_" + parts[1];
                taghfif += parseFloat($('#' + Year_id456).val().replace(/,/g, '')) || 0;

                var Year_id457 = "bedehkar31_" + parts[1];
                bytaghfif += parseFloat($('#' + Year_id456).val().replace(/,/g, '')) || 0;
                tr -= parseFloat($('#' + Year_id456).val().replace(/,/g, '')) || 0;







                console.log(tr);



            }
        });
        $('.installmentAmountInputManually302').each(function (i, obj) {
            console.log("djkjsdkl");
            let count = 0;

            if ($(this).attr("id").split("_").length == 2) {
                var obj = new Object();

                count++;
                console.log($(this).attr("id"));

                //console.log($("#select_peyman1").val());
                //console.log($("#miladi_date2").val());
                var parts = $(this).attr("id").split("_"); // Corrected line                    var inputString = $(this).val();

                //var parts = id.split("_");


                //obj.Creditorcredit = $("#bestankar").val();
                //obj.Debtorcredit = $("#bedehkar").val();
                var Year_id456 = "bedehkar323577865_" + parts[1];
                arzesh += parseFloat($('#' + Year_id456).val().replace(/,/g, '')) || 0;

                tr += parseFloat($('#' + Year_id456).val().replace(/,/g, '')) || 0;







                console.log(tr);



            }
        });

        $('#sumkol1').val(formatNumber(aval.toFixed(0)));
        $('#sumkol2').val(formatNumber(taghfif.toFixed(0)));
        $('#sumkol3').val(formatNumber(bytaghfif.toFixed(0)));
        $('#sumkol4').val(formatNumber(arzesh.toFixed(0)));

        $('#sumkol').val(formatNumber(tr.toFixed(0)));
    }

    // ترکیب فرمت‌کردن و محاسبه همزمان
    function formatAndCalculate(rowId) {
        formatNumberInput($('#bestankar_' + rowId));
        formatNumberInput($('#bedehkar3_' + rowId));
        formatNumberInput($('#bedehkar_' + rowId));
        formatNumberInput($('#bedehkar30_' + rowId));
        formatNumberInput($('#bedehkar31_' + rowId));
        formatNumberInput($('#bedehkar32_' + rowId));
        calculateTotal(rowId);
    }
    // محاسبه و به‌روزرسانی رقم کل با فرمت سه رقم سه رقم
    function calculateTotal(rowId) {
        var amount = parseFloat($('#bestankar_' + rowId).val().replace(/,/g, '')) || 0;
        var rate = parseFloat($('#bedehkar3_' + rowId).val().replace(/,/g, '')) || 0;
        var total = amount * rate;

        $('#bedehkar_' + rowId).val(formatNumber(total.toFixed(0)));



        var amount2 = parseFloat($('#bedehkar_' + rowId).val().replace(/,/g, '')) || 0;
        var rate2 = parseFloat($('#bedehkar30_' + rowId).val().replace(/,/g, '')) || 0;
        var total2 = amount2 - rate2;

        $('#bedehkar31_' + rowId).val(formatNumber(total2.toFixed(0)));

        var amount3 = parseFloat($('#bedehkar31_' + rowId).val().replace(/,/g, '')) || 0;
        var rate3 = parseFloat($('#bedehkar32_' + rowId).val().replace(/,/g, '')) || 0;
        var total3 = amount3 + rate3;
        var aval = 0;
        var taghfif = 0;
        var bytaghfif = 0;
        var arzesh = 0;
        tyu = total;
        sumpol += total;
        var tr = 0;
        $('#bedehkar33_' + rowId).val(formatNumber(total3.toFixed(0)));
        $('.installmentAmountInputManually').each(function (i, obj) {
            console.log("djkjsdkl");
            let count = 0;

            if ($(this).attr("id").split("_").length == 2) {
                var obj = new Object();

                count++;
                console.log($(this).attr("id"));

                //console.log($("#select_peyman1").val());
                //console.log($("#miladi_date2").val());
                var parts = $(this).attr("id").split("_"); // Corrected line                    var inputString = $(this).val();

                //var parts = id.split("_");


                //obj.Creditorcredit = $("#bestankar").val();
                //obj.Debtorcredit = $("#bedehkar").val();

                var Year_id4 = "bedehkar33_" + parts[1];
                tr += parseFloat($('#' + Year_id4).val().replace(/,/g, '')) || 0;
                var Year_id45 = "bedehkar_" + parts[1];
                aval += parseFloat($('#' + Year_id45).val().replace(/,/g, '')) || 0;

                var Year_id456 = "bedehkar30_" + parts[1];
                taghfif += parseFloat($('#' + Year_id456).val().replace(/,/g, '')) || 0;

                var Year_id457 = "bedehkar31_" + parts[1];
                bytaghfif += parseFloat($('#' + Year_id457).val().replace(/,/g, '')) || 0;

                var Year_id458 = "bedehkar32_" + parts[1];
                arzesh += parseFloat($('#' + Year_id458).val().replace(/,/g, '')) || 0;


                console.log(tr);

                console.log(parseFloat($('#' + Year_id4).val().replace(/,/g, '')) || 0);


            }
        });


        $('.installmentAmountInputManually3000').each(function (i, obj) {
            console.log("djkjsdkl");
            let count = 0;

            if ($(this).attr("id").split("_").length == 2) {
                var obj = new Object();

                count++;
                console.log($(this).attr("id"));

                //console.log($("#select_peyman1").val());
                //console.log($("#miladi_date2").val());
                var parts = $(this).attr("id").split("_"); // Corrected line                    var inputString = $(this).val();

                //var parts = id.split("_");


                //obj.Creditorcredit = $("#bestankar").val();
                //obj.Debtorcredit = $("#bedehkar").val();
                var Year_id456 = "bedehkar3045556_" + parts[1];
                taghfif += parseFloat($('#' + Year_id456).val().replace(/,/g, '')) || 0;

                var Year_id457 = "bedehkar31_" + parts[1];
                bytaghfif += parseFloat($('#' + Year_id456).val().replace(/,/g, '')) || 0;
                tr -= parseFloat($('#' + Year_id456).val().replace(/,/g, '')) || 0;







                console.log(tr);



            }
        });
        $('.installmentAmountInputManually302').each(function (i, obj) {
            console.log("djkjsdkl");
            let count = 0;

            if ($(this).attr("id").split("_").length == 2) {
                var obj = new Object();

                count++;
                console.log($(this).attr("id"));

                //console.log($("#select_peyman1").val());
                //console.log($("#miladi_date2").val());
                var parts = $(this).attr("id").split("_"); // Corrected line                    var inputString = $(this).val();

                //var parts = id.split("_");


                //obj.Creditorcredit = $("#bestankar").val();
                //obj.Debtorcredit = $("#bedehkar").val();
                var Year_id456 = "bedehkar323577865_" + parts[1];
                arzesh += parseFloat($('#' + Year_id456).val().replace(/,/g, '')) || 0;

                tr += parseFloat($('#' + Year_id456).val().replace(/,/g, '')) || 0;







                console.log(tr);



            }
        });

        $('#sumkol1').val(formatNumber(aval.toFixed(0)));
        $('#sumkol2').val(formatNumber(taghfif.toFixed(0)));
        $('#sumkol3').val(formatNumber(bytaghfif.toFixed(0)));
        $('#sumkol4').val(formatNumber(arzesh.toFixed(0)));

        $('#sumkol').val(formatNumber(tr.toFixed(0)));



    }
    function calculateTotal456(rowId) {

        var aval = 0;
        var taghfif = 0;
        var bytaghfif = 0;
        var arzesh = 0;

        var tr = 0;
        $('.installmentAmountInputManually').each(function (i, obj) {
            console.log("djkjsdkl");
            let count = 0;

            if ($(this).attr("id").split("_").length == 2) {
                var obj = new Object();

                count++;
                console.log($(this).attr("id"));

                //console.log($("#select_peyman1").val());
                //console.log($("#miladi_date2").val());
                var parts = $(this).attr("id").split("_"); // Corrected line                    var inputString = $(this).val();

                //var parts = id.split("_");


                //obj.Creditorcredit = $("#bestankar").val();
                //obj.Debtorcredit = $("#bedehkar").val();

                var Year_id4 = "bedehkar33_" + parts[1];
                tr += parseFloat($('#' + Year_id4).val().replace(/,/g, '')) || 0;
                var Year_id45 = "bedehkar_" + parts[1];
                aval += parseFloat($('#' + Year_id45).val().replace(/,/g, '')) || 0;

                var Year_id456 = "bedehkar30_" + parts[1];
                taghfif += parseFloat($('#' + Year_id456).val().replace(/,/g, '')) || 0;

                var Year_id457 = "bedehkar31_" + parts[1];
                bytaghfif += parseFloat($('#' + Year_id457).val().replace(/,/g, '')) || 0;

                var Year_id458 = "bedehkar32_" + parts[1];
                arzesh += parseFloat($('#' + Year_id458).val().replace(/,/g, '')) || 0;


                console.log(tr);

                console.log(parseFloat($('#' + Year_id4).val().replace(/,/g, '')) || 0);


            }
        });


        $('.installmentAmountInputManually3000').each(function (i, obj) {
            console.log("djkjsdkl");
            let count = 0;

            if ($(this).attr("id").split("_").length == 2) {
                var obj = new Object();

                count++;
                console.log($(this).attr("id"));

                //console.log($("#select_peyman1").val());
                //console.log($("#miladi_date2").val());
                var parts = $(this).attr("id").split("_"); // Corrected line                    var inputString = $(this).val();
                formatNumberInput($('#bedehkar3045556_' + parts[1]));

                //var parts = id.split("_");


                //obj.Creditorcredit = $("#bestankar").val();
                //obj.Debtorcredit = $("#bedehkar").val();
                var Year_id456 = "bedehkar3045556_" + parts[1];
                taghfif += parseFloat($('#' + Year_id456).val().replace(/,/g, '')) || 0;

                bytaghfif += parseFloat($('#' + Year_id456).val().replace(/,/g, '')) || 0;
                tr -= parseFloat($('#' + Year_id456).val().replace(/,/g, '')) || 0;







                console.log(tr);



            }
        });
        $('.installmentAmountInputManually302').each(function (i, obj) {
            console.log("djkjsdkl");
            let count = 0;

            if ($(this).attr("id").split("_").length == 2) {
                var obj = new Object();

                count++;
                console.log($(this).attr("id"));

                //console.log($("#select_peyman1").val());
                //console.log($("#miladi_date2").val());
                var parts = $(this).attr("id").split("_"); // Corrected line                    var inputString = $(this).val();
                formatNumberInput($('#bedehkar323577865_' + parts[1]));

                //var parts = id.split("_");


                //obj.Creditorcredit = $("#bestankar").val();
                //obj.Debtorcredit = $("#bedehkar").val();
                var Year_id456 = "bedehkar323577865_" + parts[1];
                arzesh += parseFloat($('#' + Year_id456).val().replace(/,/g, '')) || 0;

                tr += parseFloat($('#' + Year_id456).val().replace(/,/g, '')) || 0;







                console.log(tr);



            }
        });

        $('#sumkol1').val(formatNumber(aval.toFixed(0)));
        $('#sumkol2').val(formatNumber(taghfif.toFixed(0)));
        $('#sumkol3').val(formatNumber(bytaghfif.toFixed(0)));
        $('#sumkol4').val(formatNumber(arzesh.toFixed(0)));

        $('#sumkol').val(formatNumber(tr.toFixed(0)));



    }
    function formatAndCalculate2(rowId) {
        formatNumberInput($('#bedehkar_' + rowId));
        formatNumberInput($('#bedehkar30_' + rowId));
        calculateTotal2(rowId);
    }
    function calculateTotal2(rowId) {
        var amount = parseFloat($('#bedehkar_' + rowId).val().replace(/,/g, '')) || 0;
        var rate = parseFloat($('#bedehkar30_' + rowId).val().replace(/,/g, '')) || 0;
        var total = amount - rate;
        tyu = total;
        sumpol += total;
        $('#bedehkar31_' + rowId).val(formatNumber(total.toFixed(2)));

    }


    function formatAndCalculate23(rowId) {
        formatNumberInput($('#bedehkar31_' + rowId));
        formatNumberInput($('#bedehkar32_' + rowId));
        calculateTotal23(rowId);
    }
    function calculateTotal23(rowId) {
        var amount = parseFloat($('#bedehkar31_' + rowId).val().replace(/,/g, '')) || 0;
        var rate = parseFloat($('#bedehkar32_' + rowId).val().replace(/,/g, '')) || 0;
        var total = amount + rate;
        tyu = total;
        sumpol += total;
        $('#bedehkar33_' + rowId).val(formatNumber(total.toFixed(2)));

    }

    // فرمت کردن ورودی‌ها هنگام تایپ
    function formatNumberInput(input) {
        var value = input.val().replace(/,/g, '');
        if (!isNaN(value) && value !== '') {
            input.val(formatNumber(value));
        }
    }

    // تابع برای فرمت کردن عدد به‌صورت سه رقم سه رقم
    function formatNumber(value) {
        // جدا کردن بخش صحیح و اعشاری
        var parts = value.toString().split(".");
        parts[0] = parts[0].replace(/\B(?=(\d{3})+(?!\d))/g, ",");
        return parts.join(".");
    }
        var sabt = 1;

    $("#btnsabtavaleh403").click(function () {
        setTimeout(function () {
            // Log the value of the datepicker
            console.log($("#datepickerFromEn_Eq12").val());

            // Get the selected option
            var selectedOption = $("#naemshr option:selected");  // Get the selected option within #naemshr
            var intervalValue = selectedOption.data("html");     // Retrieve the data-html attribute
            console.log(intervalValue);

            // Check if intervalValue is null and if the class exists
            if (intervalValue == 0 && $('.installmentAmountInputManually302').length > 0) {
                confirmFinancialSummary2();
            } else {
                confirmFinancialSummary();
            }
        }, 1000); // 3-second delay
    });

    $("#btnsabtnahei1403").click(function () {
        setTimeout(function () {
            // Log the value of the datepicker
            console.log($("#datepickerFromEn_Eq12").val());

            // Get the selected option
            var selectedOption = $("#naemshr option:selected");  // Get the selected option within #naemshr
            var intervalValue = selectedOption.data("html");
            let datepickerValue = $("#datapick").val() || "null";
            let datepickerValue2 = $("#sanad").val() || "null";
            let datepickerValue4 = $("#codsamanehmoadyan").val() || "null";
            let datepickerValue3 = $("#sharhasli").val() || "null"; // Retrieve the data-html attribute



            console.log(intervalValue);

            // Print values for debugging
            console.log(datepickerValue);
            console.log(datepickerValue2);
            console.log(datepickerValue3);

            var tx = $("#pardaght").val().replace(/,/g, '') || 0;
            var tx2 = $("#sumkol").val().replace(/,/g, '') || 0;
            console.log(tx);
            console.log(tx2);
            console.log(typeof tx, tx); // خروجی: "number", 10
            console.log(typeof tx2, tx2);
            var num = parseInt(tx, 10);
            var num2 = parseInt(tx2, 10);
            console.log(typeof num2, num2);

            // چک کردن شرط‌ها
            if (datepickerValue === "null") {
                console.log(datepickerValue3);
                showSimpleAlerttarigh();
            } else if (datepickerValue2 === "null") {
                console.log(datepickerValue2);
                showSimpleAlertsanad();
            } else if (datepickerValue3 === "null") {
                console.log(datepickerValue3);
                showSimpleAlertsharh();
            } else if (datepickerValue4 === "null") {
                console.log(datepickerValue3);
                showSimpleAlertshomarehFACTOR();
            }
            // Check if intervalValue is 0 and if the class exists
            else if (intervalValue == 0 && $('.installmentAmountInputManually302').length > 0) {
                confirmFinancialSummary2nahei();
            }

            // Conditional logic based on tx and tx2
            else if (num < num2 && num != 0) {
                console.log(tx);
                console.log(tx2);
                showSimpleAlert(); // Logic to handle when tx is less than tx2
            } else {
                confirmFinancialSummarynahaei();
            }
        }, 1000); // 3-second delay
    });

// Function to handle the confirmation dialog
    function confirmFinancialSummary() {
        console.log("confirmFinancialSummary");

    Swal.fire({
        title: 'حمع کل سند مالی شما برابر ' + $("#sumkol").val() + ' مطمئن هستید؟',
        icon: 'question',
        showCancelButton: true,
        background: 'rgb(29,40,52)',
        color: 'white',
        confirmButtonColor: '#3085d6',
        cancelButtonColor: '#d33',
        confirmButtonText: 'بله',
        cancelButtonText: 'خیر'
    }).then((result) => {
        if (result.isConfirmed) {
            $(".loader").css('display', 'block');
            var fd45 = new FormData();
            sendAjax3000900('@Url.Action("creatforamaasnadsherkatedit", "FinancialDocuments", new { area = "ManagementAccounting" })', fd45);
        }
    });
}
    function confirmFinancialSummary2() {
        console.log("confirmFinancialSummary2");

    Swal.fire({
        title: 'برای نام فروشنده کد اقتصادی ثبت  نشده مطمین هستید؟',
        icon: 'question',
        showCancelButton: true,
        background: 'rgb(29,40,52)',
        color: 'white',
        confirmButtonColor: '#3085d6',
        cancelButtonColor: '#d33',
        confirmButtonText: 'بله',
        cancelButtonText: 'خیر'
    }).then((result) => {
        if (result.isConfirmed) {
                         confirmFinancialSummary();

        }
    });
}
     function confirmFinancialSummarynahaei() {
     console.log("confirmFinancialSummary");

 Swal.fire({
     //title: 'حمع کل سند مالی شما برابر ' + $("#sumkol").val() + ' مطمئن هستید؟',
     title: 'آیا از تغییرات خود مطمین هستید؟',

     icon: 'question',
     showCancelButton: true,
     background: 'rgb(29,40,52)',
     color: 'white',
     confirmButtonColor: '#3085d6',
     cancelButtonColor: '#d33',
     confirmButtonText: 'بله',
     cancelButtonText: 'خیر'
 }).then((result) => {
     if (result.isConfirmed) {
         $(".loader").css('display', 'block');
         var fd45 = new FormData();
         sendAjax30009001111('@Url.Action("creatforamaasnadsherkateditnahei", "FinancialDocuments", new { area = "ManagementAccounting" })', fd45);
     }ا
 });
 }
    function confirmFinancialSummary2nahei() {
        console.log("confirmFinancialSummary2");

        Swal.fire({
            title: 'برای نام فروشنده کد اقتصادی ثبت  نشده مطمین هستید؟',
            icon: 'question',
            showCancelButton: true,
            background: 'rgb(29,40,52)',
            color: 'white',
            confirmButtonColor: '#3085d6',
            cancelButtonColor: '#d33',
            confirmButtonText: 'بله',
            cancelButtonText: 'خیر'
        }).then((result) => {
            if (result.isConfirmed) {
                confirmFinancialSummarynahaei();

            }
        });
    }
    function confirmFinancialSummary3nahei() {
        console.log("confirmFinancialSummary2");

        Swal.fire({
            title: 'مبلغ پرداختی کمتر است از مبلغ نهایی؟',
            icon: 'question',
            showCancelButton: true,
            background: 'rgb(29,40,52)',
            color: 'white',
            confirmButtonColor: '#3085d6',
            cancelButtonColor: '#d33',
            confirmButtonText: 'بله',
            cancelButtonText: 'خیر'
        }).then((result) => {
            if (result.isConfirmed) {
                confirmFinancialSummarynahaei();

            }
        });
    }
    function sendAjax3000900(url2, fd2) {
        // fd2 = new FormData(); // Initialize FormData inside the function
        var fd30 = new FormData(); // Initialize FormData inside the function

        fd.forEach((value, key) => {
            fd30.append(key, value);
        });
        fd30.append("sharhasli", $("#sharhasli").val());
        fd30.append("endfalse", $("#naemshr").val());
        fd30.append("DataDocument", $("#datapick").val());
        fd30.append("summm", $("#sumkol").val());
        fd30.append("FK_usr", $("#namefard").val());
        fd30.append("DataDocument2", $("#datapick2").val());
        fd30.append("shomarehpardaght", $("#shomarehpardaght").val());
        fd30.append("sanad", $("#sanad").val());
        fd30.append("pardaght2", $("#pardaght").val().replace(/,/g, '') || 0);
        fd30.append("codsamanehmoadyan", $("#codsamanehmoadyan").val());
        fd30.append("sabt", sabt);
        fd30.append("idd", @fin.ID);

        console.log($("#sanad").val());
        console.log(fd2);

            console.log($("#namefard").val());

        $.ajax({
        url: url2,
        type: 'POST',
            data: fd30,
        processData: false,
        contentType: false,
            success: function (resp1) {

                if (resp1 === "-6") {
                    Swal.fire({
                        title: " شماره سندهای ذیل خالی هستند ، آیا از انتخاب خود مطمئن هستید ؟",
                        icon: 'question',
                        showCancelButton: true,
                        showDenyButton: true,
                        background: 'rgb(29,40,52)',
                        color: 'white',
                        confirmButtonColor: '#3085d6',
                        cancelButtonColor: '#d33',
                        denyButtonColor: '#6c757d',
                        confirmButtonText: 'بله',
                        cancelButtonText: 'خیر',
                        denyButtonText: 'مشاهده شماره سندهای خالی'
                    }).then((result) => {
                        if (result.isConfirmed) {
                            sabt = 0;
                            sendAjax30009000000('@Url.Action("creatforamaasnadsherkatedit", "FinancialDocuments", new { area = "ManagementAccounting" })', fd);
                        }
                        else if (result.isDenied) {
                            // عملکرد مربوط به گزینه مشاهده
                            //getlist();
                @*sendAjax5('@Url.Action("viewdetail", "RegistrationDocument",new { area = "Contracts" })');*@
                            //var url = "/RegistrationDocument/viewdetail";
                            //$("#big_modal_body1").load(url);
                            $("#myModal3").addClass("fade-in"); // Add the fade-in class
                            $("#myModal3").css("display", "block");
                            $("#ss12456").load('@Url.Content("/ManagementAccounting/FinancialDocuments/viewforsanadnumber")?sanad=' + $("#sanad").val());

                            // اینجا کدی که برای مشاهده باید اجرا شود را بنویسید
                        }


                        else if (result.isDismissed && result.dismiss === Swal.DismissReason.cancel) {




                            // عملکرد مربوط به گزینه خیر
                @*sendAjax533('@Url.Action("deletee", "RegistrationDocument",new { area = "Contracts" })');*@
                            // اینجا کدی که برای گزینه "خیر" باید اجرا شود را بنویسید
                        }
                    });

                }
                else if (resp1 === "-7") {
                    Swal.fire({
                        title: "قبلا سندی با این شماره ثبت شده است ، آیا میخواهید محتوای این فاکتور به سند مذکور اضافه شود؟",
                        icon: 'question',
                        showCancelButton: true,
                        showDenyButton: true,
                        background: 'rgb(29,40,52)',
                        color: 'white',
                        confirmButtonColor: '#3085d6',
                        cancelButtonColor: '#d33',
                        denyButtonColor: '#6c757d',
                        confirmButtonText: 'بله',
                        cancelButtonText: 'خیر',
                        denyButtonText: ' مشاهده محتوای سند ثبت شده'
                    }).then((result) => {
                        if (result.isConfirmed) {
                            sabt = 0;
                            sendAjax30009000000('@Url.Action("creatforamaasnadsherkatedit", "FinancialDocuments", new { area = "ManagementAccounting" })', fd);
                        }
                        else if (result.isDenied) {
                            // عملکرد مربوط به گزینه مشاهده
                            //getlist();
                   @*sendAjax5('@Url.Action("viewdetail", "RegistrationDocument",new { area = "Contracts" })');*@
                            //var url = "/RegistrationDocument/viewdetail";
                            //$("#big_modal_body1").load(url);
                            $("#myModal2").addClass("fade-in"); // Add the fade-in class
                            $("#myModal2").css("display", "block");
                            $("#ss").load('@Url.Content("/ManagementAccounting/FinancialDocuments/viewsanad")?sanad=' + $("#sanad").val());

                            // اینجا کدی که برای مشاهده باید اجرا شود را بنویسید
                        }


                        else if (result.isDismissed && result.dismiss === Swal.DismissReason.cancel) {




                            // عملکرد مربوط به گزینه خیر
                   @*sendAjax533('@Url.Action("deletee", "RegistrationDocument",new { area = "Contracts" })');*@
                            // اینجا کدی که برای گزینه "خیر" باید اجرا شود را بنویسید
                        }
                    });
                }

                else {
                    console.log("okeye");
console.log(resp1);
$("#loading-message").css("display", "none");
fd = new FormData();
var Filters = [];

console.log("djkjsdkl");

$('.installmentAmountInputManually').each(function (i, obj) {
console.log("djkjsdkl");
let count = 0;

if ($(this).attr("id").split("_").length == 2) {
var obj = new Object();
count++;
console.log($(this).attr("id"));

var parts = $(this).attr("id").split("_");
obj.FK_nameshr = $(this).val();

var Year_id4 = "bedehkar3_" + parts[1];
//var val2 = $('#datepickerFromEn_Eq12323_' + parts[1]).val();
var Monthid = "bedehkar_" + parts[1];
var inputString = $("#" + Monthid).val().replace(/,/g, '') || 0;;
var inputString2 = $("#bestankar_" + parts[1]).val().replace(/,/g, '') || 0;;

obj.count = inputString2;
obj.vahd = $("#" + Year_id4).val().replace(/,/g, '') || 0;;
//obj.Title = ;
obj.FK_asliasnad = resp1;
obj.megdar = inputString;  // بدون

    var inputString200 = $("#bedehkar30_" + parts[1]).val().replace(/,/g, '') || 0;;
    var bytaghfif = $("#bedehkar31_" + parts[1]).val().replace(/,/g, '') || 0;;
    var arzish = $("#bedehkar32_" + parts[1]).val().replace(/,/g, '') || 0;;
    var nahaei = $("#bedehkar33_" + parts[1]).val().replace(/,/g, '') || 0;;
    obj.taghfif = inputString200;
    obj.bytaghfif = bytaghfif;
    obj.arzish = arzish;
    obj.nahaei = nahaei;




Filters.push(obj);
}
});
    $('.installmentAmountInputManually3000').each(function (i, obj) {
        console.log("djkjsdkl");
        let count = 0;

        if ($(this).attr("id").split("_").length == 2) {
            var obj = new Object();

            count++;
            console.log($(this).attr("id"));

            //console.log($("#select_peyman1").val());
            //console.log($("#miladi_date2").val());
            var parts = $(this).attr("id").split("_"); // Corrected line                    var inputString = $(this).val();

            //var parts = id.split("_");


            //obj.Creditorcredit = $("#bestankar").val();
            //obj.Debtorcredit = $("#bedehkar").val();
            var Year_id456 = "bedehkar3045556_" + parts[1];
            obj.taghfif = parseFloat($('#' + Year_id456).val().replace(/,/g, '')) || 0;
            obj.FK_asliasnad = resp1;
            var Year_id45688 = "bedehkar300000_" + parts[1];
            var Year_id4568833334 = "creditline2wt345_" + parts[1];
            obj.FK_nameshr = $('#' + Year_id4568833334).val()
            obj.sharhtaghfif = $('#' + Year_id45688).val()



            Filters.push(obj);







        }
    });
    $('.installmentAmountInputManually302').each(function (i, obj) {
        console.log("djkjsdkl");
        let count = 0;

        if ($(this).attr("id").split("_").length == 2) {
            var obj = new Object();

            count++;
            console.log($(this).attr("id"));

            //console.log($("#select_peyman1").val());
            //console.log($("#miladi_date2").val());
            var parts = $(this).attr("id").split("_"); // Corrected line                    var inputString = $(this).val();

            //var parts = id.split("_");


            //obj.Creditorcredit = $("#bestankar").val();
            //obj.Debtorcredit = $("#bedehkar").val();
            var Year_id456 = "bedehkar323577865_" + parts[1];
            obj.arzish = parseFloat($('#' + Year_id456).val().replace(/,/g, '')) || 0;
            obj.FK_asliasnad = resp1;
            var Year_id456788 = "bedehkar300000000_" + parts[1];
            var Year_id4568833334 = "creditline2w345_" + parts[1];

            obj.FK_nameshr = $('#' + Year_id4568833334).val()
            obj.sharharzesh = $('#' + Year_id456788).val()
            Filters.push(obj);










        }
    });
sendAjax12('@Url.Action("creatfktitlfkasnadedit", "FinancialDocuments", new { area = "ManagementAccounting" })', Filters);
                }




















        },
        error: function (resp1) {
        console.log(resp1);
        console.log("Nokeye");
        $("#loading-message").css("display", "none");
        Toast.fire({
        icon: 'success',
        background: '#35434A',
        color: 'white',
        title: "با خطا مواجه شده است ، لطفا بعدا تلاش فرمایید"
        }).then(() => {
        $("#loading-message").css("display", "none");
        });
        }
        });
    }
    function sendAjax30009001111(url2, fd2) {
         //fd2 = new FormData(); // Initialize FormData inside the function
        var fd30 = new FormData(); // Initialize FormData inside the function

        fd.forEach((value, key) => {
            fd30.append(key, value);
        });
        fd30.append("sharhasli", $("#sharhasli").val());
        fd30.append("endfalse", $("#naemshr").val());
        fd30.append("DataDocument", $("#datapick").val());
        fd30.append("summm", $("#sumkol").val());
        fd30.append("FK_usr", $("#namefard").val());
        fd30.append("DataDocument2", $("#datapick2").val());
        fd30.append("shomarehpardaght", $("#shomarehpardaght").val());
        fd30.append("sanad", $("#sanad").val());
        fd30.append("pardaght2", $("#pardaght").val().replace(/,/g, '') || 0);
        fd30.append("codsamanehmoadyan", $("#codsamanehmoadyan").val());
        fd30.append("sabt", sabt);
        fd30.append("idd", @fin.ID);

        console.log($("#sanad").val());
        console.log(fd30);

            console.log($("#namefard").val());

        $.ajax({
        url: url2,
        type: 'POST',
            data: fd30,
        processData: false,
        contentType: false,
            success: function (resp1) {

                if (resp1 === "-6") {
                    Swal.fire({
                        title: " شماره سندهای ذیل خالی هستند ، آیا از انتخاب خود مطمئن هستید ؟",
                        icon: 'question',
                        showCancelButton: true,
                        showDenyButton: true,
                        background: 'rgb(29,40,52)',
                        color: 'white',
                        confirmButtonColor: '#3085d6',
                        cancelButtonColor: '#d33',
                        denyButtonColor: '#6c757d',
                        confirmButtonText: 'بله',
                        cancelButtonText: 'خیر',
                        denyButtonText: 'مشاهده شماره سندهای خالی'
                    }).then((result) => {
                        if (result.isConfirmed) {
                            sabt = 0;
                            sendAjax30009000000('@Url.Action("creatforamaasnadsherkateditnahei", "FinancialDocuments", new { area = "ManagementAccounting" })', fd);
                        }
                        else if (result.isDenied) {
                            // عملکرد مربوط به گزینه مشاهده
                            //getlist();
                @*sendAjax5('@Url.Action("viewdetail", "RegistrationDocument",new { area = "Contracts" })');*@
                            //var url = "/RegistrationDocument/viewdetail";
                            //$("#big_modal_body1").load(url);
                            $("#myModal3").addClass("fade-in"); // Add the fade-in class
                            $("#myModal3").css("display", "block");
                            $("#ss12456").load('@Url.Content("/ManagementAccounting/FinancialDocuments/viewforsanadnumber")?sanad=' + $("#sanad").val());

                            // اینجا کدی که برای مشاهده باید اجرا شود را بنویسید
                        }


                        else if (result.isDismissed && result.dismiss === Swal.DismissReason.cancel) {




                            // عملکرد مربوط به گزینه خیر
                @*sendAjax533('@Url.Action("deletee", "RegistrationDocument",new { area = "Contracts" })');*@
                            // اینجا کدی که برای گزینه "خیر" باید اجرا شود را بنویسید
                        }
                    });

                }
                else if (resp1 === "-7") {
                    Swal.fire({
                        title: "قبلا سندی با این شماره ثبت شده است ، آیا میخواهید محتوای این فاکتور به سند مذکور اضافه شود؟",
                        icon: 'question',
                        showCancelButton: true,
                        showDenyButton: true,
                        background: 'rgb(29,40,52)',
                        color: 'white',
                        confirmButtonColor: '#3085d6',
                        cancelButtonColor: '#d33',
                        denyButtonColor: '#6c757d',
                        confirmButtonText: 'بله',
                        cancelButtonText: 'خیر',
                        denyButtonText: ' مشاهده محتوای سند ثبت شده'
                    }).then((result) => {
                        if (result.isConfirmed) {
                            sabt = 0;
                            sendAjax30009000000('@Url.Action("creatforamaasnadsherkateditnahei", "FinancialDocuments", new { area = "ManagementAccounting" })', fd);
                        }
                        else if (result.isDenied) {
                            // عملکرد مربوط به گزینه مشاهده
                            //getlist();
                   @*sendAjax5('@Url.Action("viewdetail", "RegistrationDocument",new { area = "Contracts" })');*@
                            //var url = "/RegistrationDocument/viewdetail";
                            //$("#big_modal_body1").load(url);
                            $("#myModal2").addClass("fade-in"); // Add the fade-in class
                            $("#myModal2").css("display", "block");
                            $("#ss").load('@Url.Content("/ManagementAccounting/FinancialDocuments/viewsanad")?sanad=' + $("#sanad").val());

                            // اینجا کدی که برای مشاهده باید اجرا شود را بنویسید
                        }


                        else if (result.isDismissed && result.dismiss === Swal.DismissReason.cancel) {




                            // عملکرد مربوط به گزینه خیر
                   @*sendAjax533('@Url.Action("deletee", "RegistrationDocument",new { area = "Contracts" })');*@
                            // اینجا کدی که برای گزینه "خیر" باید اجرا شود را بنویسید
                        }
                    });
                }

                else {
                    console.log("okeye");
console.log(resp1);
$("#loading-message").css("display", "none");
fd = new FormData();
var Filters = [];

console.log("djkjsdkl");

$('.installmentAmountInputManually').each(function (i, obj) {
console.log("djkjsdkl");
let count = 0;

if ($(this).attr("id").split("_").length == 2) {
var obj = new Object();
count++;
console.log($(this).attr("id"));

var parts = $(this).attr("id").split("_");
obj.FK_nameshr = $(this).val();

var Year_id4 = "bedehkar3_" + parts[1];
//var val2 = $('#datepickerFromEn_Eq12323_' + parts[1]).val();
var Monthid = "bedehkar_" + parts[1];
var inputString = $("#" + Monthid).val().replace(/,/g, '') || 0;;
var inputString2 = $("#bestankar_" + parts[1]).val().replace(/,/g, '') || 0;;

obj.count = inputString2;
obj.vahd = $("#" + Year_id4).val().replace(/,/g, '') || 0;;
//obj.Title = ;
obj.FK_asliasnad = resp1;
obj.megdar = inputString;  // بدون

    var inputString200 = $("#bedehkar30_" + parts[1]).val().replace(/,/g, '') || 0;;
    var bytaghfif = $("#bedehkar31_" + parts[1]).val().replace(/,/g, '') || 0;;
    var arzish = $("#bedehkar32_" + parts[1]).val().replace(/,/g, '') || 0;;
    var nahaei = $("#bedehkar33_" + parts[1]).val().replace(/,/g, '') || 0;;
    obj.taghfif = inputString200;
    obj.bytaghfif = bytaghfif;
    obj.arzish = arzish;
    obj.nahaei = nahaei;




Filters.push(obj);
}
});
    $('.installmentAmountInputManually3000').each(function (i, obj) {
        console.log("djkjsdkl");
        let count = 0;

        if ($(this).attr("id").split("_").length == 2) {
            var obj = new Object();

            count++;
            console.log($(this).attr("id"));

            //console.log($("#select_peyman1").val());
            //console.log($("#miladi_date2").val());
            var parts = $(this).attr("id").split("_"); // Corrected line                    var inputString = $(this).val();

            //var parts = id.split("_");


            //obj.Creditorcredit = $("#bestankar").val();
            //obj.Debtorcredit = $("#bedehkar").val();
            var Year_id456 = "bedehkar3045556_" + parts[1];
            obj.taghfif = parseFloat($('#' + Year_id456).val().replace(/,/g, '')) || 0;
            obj.FK_asliasnad = resp1;
            var Year_id45688 = "bedehkar300000_" + parts[1];

            obj.sharhtaghfif = $('#' + Year_id45688).val()

            var Year_id4568833334 = "creditline2wt345_" + parts[1];
            obj.FK_nameshr = $('#' + Year_id4568833334).val()

            Filters.push(obj);







        }
    });
    $('.installmentAmountInputManually302').each(function (i, obj) {
        console.log("djkjsdkl");
        let count = 0;

        if ($(this).attr("id").split("_").length == 2) {
            var obj = new Object();

            count++;
            console.log($(this).attr("id"));

            //console.log($("#select_peyman1").val());
            //console.log($("#miladi_date2").val());
            var parts = $(this).attr("id").split("_"); // Corrected line                    var inputString = $(this).val();

            //var parts = id.split("_");


            //obj.Creditorcredit = $("#bestankar").val();
            //obj.Debtorcredit = $("#bedehkar").val();
            var Year_id456 = "bedehkar323577865_" + parts[1];
            obj.arzish = parseFloat($('#' + Year_id456).val().replace(/,/g, '')) || 0;
            obj.FK_asliasnad = resp1;
            var Year_id456788 = "bedehkar300000000_" + parts[1];
            obj.sharharzesh = $('#' + Year_id456788).val()
            var Year_id4568833334 = "creditline2w345_" + parts[1];

            obj.FK_nameshr = $('#' + Year_id4568833334).val()
            Filters.push(obj);










        }
    });
sendAjax12('@Url.Action("creatfktitlfkasnadedit", "FinancialDocuments", new { area = "ManagementAccounting" })', Filters);
                }




















        },
        error: function (resp1) {
        console.log(resp1);
        console.log("Nokeye");
        $("#loading-message").css("display", "none");
        Toast.fire({
        icon: 'success',
        background: '#35434A',
        color: 'white',
        title: "با خطا مواجه شده است ، لطفا بعدا تلاش فرمایید"
        }).then(() => {
        $("#loading-message").css("display", "none");
        });
        }
        });
        }

    function sendAjax30009000000(url2, fd2) {
        //fd2 = new FormData(); // Initialize FormData inside the function
        var fd30 = new FormData(); // Initialize FormData inside the function

        fd.forEach((value, key) => {
            fd30.append(key, value);
        });
        sabt = 0;
        fd30.append("sharhasli", $("#sharhasli").val());
        fd30.append("endfalse", $("#naemshr").val());
        fd30.append("DataDocument", $("#datapick").val());
        fd30.append("summm", $("#sumkol").val());
        fd30.append("FK_usr", $("#namefard").val());
        fd30.append("DataDocument2", $("#datapick2").val());
        fd30.append("shomarehpardaght", $("#shomarehpardaght").val());
        fd30.append("sanad", $("#sanad").val());
        fd30.append("pardaght2", $("#pardaght").val().replace(/,/g, '') || 0);
        fd30.append("codsamanehmoadyan", $("#codsamanehmoadyan").val());
        fd30.append("sabt", sabt);
        fd30.append("idd", @fin.ID);

                    console.log("sendAjax30009000000");
        console.log(sabt);
        console.log(fd30.sabt);

            console.log($("#namefard").val());

        $.ajax({
        url: url2,
        type: 'POST',
            data: fd30,
        processData: false,
        contentType: false,
            success: function (resp1) {

                if (resp1 === "-6") {
                    Swal.fire({
                        title: " شماره سندهای ذیل خالی هستند ، آیا از انتخاب خود مطمئن هستید ؟",
                        icon: 'question',
                        showCancelButton: true,
                        showDenyButton: true,
                        background: 'rgb(29,40,52)',
                        color: 'white',
                        confirmButtonColor: '#3085d6',
                        cancelButtonColor: '#d33',
                        denyButtonColor: '#6c757d',
                        confirmButtonText: 'بله',
                        cancelButtonText: 'خیر',
                        denyButtonText: 'مشاهده شماره سندهای خالی'
                    }).then((result) => {
                        if (result.isConfirmed) {
                            sabt = 0;
                            sendAjax30009000000('@Url.Action("creatforamaasnadsherkatedit", "FinancialDocuments", new { area = "ManagementAccounting" })', fd);
                        }
                        else if (result.isDenied) {
                            // عملکرد مربوط به گزینه مشاهده
                            //getlist();
                @*sendAjax5('@Url.Action("viewdetail", "RegistrationDocument",new { area = "Contracts" })');*@
                            //var url = "/RegistrationDocument/viewdetail";
                            //$("#big_modal_body1").load(url);
                            $("#myModal2").addClass("fade-in"); // Add the fade-in class
                            $("#myModal2").css("display", "block");
                            $("#ss").load('@Url.Content("/Contracts/RegistrationDocument/viewdetail")');

                            // اینجا کدی که برای مشاهده باید اجرا شود را بنویسید
                        }


                        else if (result.isDismissed && result.dismiss === Swal.DismissReason.cancel) {




                            // عملکرد مربوط به گزینه خیر
                @*sendAjax533('@Url.Action("deletee", "RegistrationDocument",new { area = "Contracts" })');*@
                            // اینجا کدی که برای گزینه "خیر" باید اجرا شود را بنویسید
                        }
                    });

                }
                else if (resp1 === "-7") {
                    Swal.fire({
                        title: "قبلا سندی با این شماره ثبت شده است ، آیا میخواهید محتوای این فاکتور به سند مذکور اضافه شود؟",
                        icon: 'question',
                        showCancelButton: true,
                        showDenyButton: true,
                        background: 'rgb(29,40,52)',
                        color: 'white',
                        confirmButtonColor: '#3085d6',
                        cancelButtonColor: '#d33',
                        denyButtonColor: '#6c757d',
                        confirmButtonText: 'بله',
                        cancelButtonText: 'خیر',
                        denyButtonText: ' مشاهده محتوای سند ثبت شده'
                    }).then((result) => {
                        if (result.isConfirmed) {
                            sabt = 0;
                            sendAjax30009000000('@Url.Action("creatforamaasnadsherkatedit", "FinancialDocuments", new { area = "ManagementAccounting" })', fd);
                        }
                        else if (result.isDenied) {
                            // عملکرد مربوط به گزینه مشاهده
                            //getlist();
                   @*sendAjax5('@Url.Action("viewdetail", "RegistrationDocument",new { area = "Contracts" })');*@
                            //var url = "/RegistrationDocument/viewdetail";
                            //$("#big_modal_body1").load(url);
                            $("#myModal2").addClass("fade-in"); // Add the fade-in class
                            $("#myModal2").css("display", "block");
                            $("#ss").load('@Url.Content("/ManagementAccounting/FinancialDocuments/viewsanad")?sanad=' + $("#sanad").val());

                            // اینجا کدی که برای مشاهده باید اجرا شود را بنویسید
                        }


                        else if (result.isDismissed && result.dismiss === Swal.DismissReason.cancel) {




                            // عملکرد مربوط به گزینه خیر
                   @*sendAjax533('@Url.Action("deletee", "RegistrationDocument",new { area = "Contracts" })');*@
                            // اینجا کدی که برای گزینه "خیر" باید اجرا شود را بنویسید
                        }
                    });
                }

                else {
                    console.log("okeye");
console.log(resp1);
$("#loading-message").css("display", "none");
fd = new FormData();
var Filters = [];

console.log("djkjsdkl");

$('.installmentAmountInputManually').each(function (i, obj) {
console.log("djkjsdkl");
let count = 0;

if ($(this).attr("id").split("_").length == 2) {
var obj = new Object();
count++;
console.log($(this).attr("id"));

var parts = $(this).attr("id").split("_");
obj.FK_nameshr = $(this).val();

var Year_id4 = "bedehkar3_" + parts[1];
//var val2 = $('#datepickerFromEn_Eq12323_' + parts[1]).val();
var Monthid = "bedehkar_" + parts[1];
var inputString = $("#" + Monthid).val().replace(/,/g, '') || 0;;
var inputString2 = $("#bestankar_" + parts[1]).val().replace(/,/g, '') || 0;;

obj.count = inputString2;
obj.vahd = $("#" + Year_id4).val().replace(/,/g, '') || 0;;
//obj.Title = ;
obj.FK_asliasnad = resp1;
obj.megdar = inputString;  // بدون

    var inputString200 = $("#bedehkar30_" + parts[1]).val().replace(/,/g, '') || 0;;
    var bytaghfif = $("#bedehkar31_" + parts[1]).val().replace(/,/g, '') || 0;;
    var arzish = $("#bedehkar32_" + parts[1]).val().replace(/,/g, '') || 0;;
    var nahaei = $("#bedehkar33_" + parts[1]).val().replace(/,/g, '') || 0;;
    obj.taghfif = inputString200;
    obj.bytaghfif = bytaghfif;
    obj.arzish = arzish;
    obj.nahaei = nahaei;




Filters.push(obj);
}
});
    $('.installmentAmountInputManually3000').each(function (i, obj) {
        console.log("djkjsdkl");
        let count = 0;

        if ($(this).attr("id").split("_").length == 2) {
            var obj = new Object();

            count++;
            console.log($(this).attr("id"));

            //console.log($("#select_peyman1").val());
            //console.log($("#miladi_date2").val());
            var parts = $(this).attr("id").split("_"); // Corrected line                    var inputString = $(this).val();

            //var parts = id.split("_");


            //obj.Creditorcredit = $("#bestankar").val();
            //obj.Debtorcredit = $("#bedehkar").val();
            var Year_id456 = "bedehkar3045556_" + parts[1];
            obj.taghfif = parseFloat($('#' + Year_id456).val().replace(/,/g, '')) || 0;
            obj.FK_asliasnad = resp1;
            var Year_id45688 = "bedehkar300000_" + parts[1];

            obj.sharhtaghfif = $('#' + Year_id45688).val()
            var Year_id4568833334 = "creditline2wt345_" + parts[1];
            obj.FK_nameshr = $('#' + Year_id4568833334).val()


            Filters.push(obj);







        }
    });
    $('.installmentAmountInputManually302').each(function (i, obj) {
        console.log("djkjsdkl");
        let count = 0;

        if ($(this).attr("id").split("_").length == 2) {
            var obj = new Object();

            count++;
            console.log($(this).attr("id"));

            //console.log($("#select_peyman1").val());
            //console.log($("#miladi_date2").val());
            var parts = $(this).attr("id").split("_"); // Corrected line                    var inputString = $(this).val();

            //var parts = id.split("_");


            //obj.Creditorcredit = $("#bestankar").val();
            //obj.Debtorcredit = $("#bedehkar").val();
            var Year_id456 = "bedehkar323577865_" + parts[1];
            obj.arzish = parseFloat($('#' + Year_id456).val().replace(/,/g, '')) || 0;
            obj.FK_asliasnad = resp1;
            var Year_id456788 = "bedehkar300000000_" + parts[1];
            obj.sharharzesh = $('#' + Year_id456788).val()
            var Year_id4568833334 = "creditline2w345_" + parts[1];

            obj.FK_nameshr = $('#' + Year_id4568833334).val()
            Filters.push(obj);










        }
    });
sendAjax12('@Url.Action("creatfktitlfkasnad", "FinancialDocuments", new { area = "ManagementAccounting" })', Filters);
                }




















        },
        error: function (resp1) {
        console.log(resp1);
        console.log("Nokeye");
        $("#loading-message").css("display", "none");
        Toast.fire({
        icon: 'success',
        background: '#35434A',
        color: 'white',
        title: "با خطا مواجه شده است ، لطفا بعدا تلاش فرمایید"
        }).then(() => {
        $("#loading-message").css("display", "none");
        });
        }
        });
    }
    function sendAjax3000900000011111(url2, fd2) {
        //fd2 = new FormData(); // Initialize FormData inside the function
        var fd30 = new FormData(); // Initialize FormData inside the function

        fd.forEach((value, key) => {
            fd30.append(key, value);
        });
        sabt = 0;
        fd30.append("sharhasli", $("#sharhasli").val());
        fd30.append("endfalse", $("#naemshr").val());
        fd30.append("DataDocument", $("#datapick").val());
        fd30.append("summm", $("#sumkol").val());
        fd30.append("FK_usr", $("#namefard").val());
        fd30.append("DataDocument2", $("#datapick2").val());
        fd30.append("shomarehpardaght", $("#shomarehpardaght").val());
        fd30.append("sanad", $("#sanad").val());
        fd30.append("pardaght2", $("#pardaght").val().replace(/,/g, '') || 0);
        fd30.append("codsamanehmoadyan", $("#codsamanehmoadyan").val());
        fd30.append("sabt", sabt);
        fd30.append("idd", @fin.ID);

                    console.log("sendAjax30009000000");
        console.log(sabt);
        console.log(fd2.sabt);

            console.log($("#namefard").val());

        $.ajax({
        url: url2,
        type: 'POST',
            data: fd30,
        processData: false,
        contentType: false,
            success: function (resp1) {

                if (resp1 === "-6") {
                    Swal.fire({
                        title: " شماره سندهای ذیل خالی هستند ، آیا از انتخاب خود مطمئن هستید ؟",
                        icon: 'question',
                        showCancelButton: true,
                        showDenyButton: true,
                        background: 'rgb(29,40,52)',
                        color: 'white',
                        confirmButtonColor: '#3085d6',
                        cancelButtonColor: '#d33',
                        denyButtonColor: '#6c757d',
                        confirmButtonText: 'بله',
                        cancelButtonText: 'خیر',
                        denyButtonText: 'مشاهده شماره سندهای خالی'
                    }).then((result) => {
                        if (result.isConfirmed) {
                            sabt = 0;
                            sendAjax30009000000('@Url.Action("creatforamaasnadsherkateditnahei", "FinancialDocuments", new { area = "ManagementAccounting" })', fd);
                        }
                        else if (result.isDenied) {
                            // عملکرد مربوط به گزینه مشاهده
                            //getlist();
                @*sendAjax5('@Url.Action("viewdetail", "RegistrationDocument",new { area = "Contracts" })');*@
                            //var url = "/RegistrationDocument/viewdetail";
                            //$("#big_modal_body1").load(url);
                            $("#myModal2").addClass("fade-in"); // Add the fade-in class
                            $("#myModal2").css("display", "block");
                            $("#ss").load('@Url.Content("/Contracts/RegistrationDocument/viewdetail")');

                            // اینجا کدی که برای مشاهده باید اجرا شود را بنویسید
                        }


                        else if (result.isDismissed && result.dismiss === Swal.DismissReason.cancel) {




                            // عملکرد مربوط به گزینه خیر
                @*sendAjax533('@Url.Action("deletee", "RegistrationDocument",new { area = "Contracts" })');*@
                            // اینجا کدی که برای گزینه "خیر" باید اجرا شود را بنویسید
                        }
                    });

                }
                else if (resp1 === "-7") {
                    Swal.fire({
                        title: "قبلا سندی با این شماره ثبت شده است ، آیا میخواهید محتوای این فاکتور به سند مذکور اضافه شود؟",
                        icon: 'question',
                        showCancelButton: true,
                        showDenyButton: true,
                        background: 'rgb(29,40,52)',
                        color: 'white',
                        confirmButtonColor: '#3085d6',
                        cancelButtonColor: '#d33',
                        denyButtonColor: '#6c757d',
                        confirmButtonText: 'بله',
                        cancelButtonText: 'خیر',
                        denyButtonText: ' مشاهده محتوای سند ثبت شده'
                    }).then((result) => {
                        if (result.isConfirmed) {
                            sabt = 0;
                            sendAjax30009000000('@Url.Action("creatforamaasnadsherkateditnahei", "FinancialDocuments", new { area = "ManagementAccounting" })', fd);
                        }
                        else if (result.isDenied) {
                            // عملکرد مربوط به گزینه مشاهده
                            //getlist();
                   @*sendAjax5('@Url.Action("viewdetail", "RegistrationDocument",new { area = "Contracts" })');*@
                            //var url = "/RegistrationDocument/viewdetail";
                            //$("#big_modal_body1").load(url);
                            $("#myModal2").addClass("fade-in"); // Add the fade-in class
                            $("#myModal2").css("display", "block");
                            $("#ss").load('@Url.Content("/ManagementAccounting/FinancialDocuments/viewsanad")?sanad=' + $("#sanad").val());

                            // اینجا کدی که برای مشاهده باید اجرا شود را بنویسید
                        }


                        else if (result.isDismissed && result.dismiss === Swal.DismissReason.cancel) {




                            // عملکرد مربوط به گزینه خیر
                   @*sendAjax533('@Url.Action("deletee", "RegistrationDocument",new { area = "Contracts" })');*@
                            // اینجا کدی که برای گزینه "خیر" باید اجرا شود را بنویسید
                        }
                    });
                }

                else {
                    console.log("okeye");
console.log(resp1);
$("#loading-message").css("display", "none");
fd = new FormData();
var Filters = [];

console.log("djkjsdkl");

$('.installmentAmountInputManually').each(function (i, obj) {
console.log("djkjsdkl");
let count = 0;

if ($(this).attr("id").split("_").length == 2) {
var obj = new Object();
count++;
console.log($(this).attr("id"));

var parts = $(this).attr("id").split("_");
obj.FK_nameshr = $(this).val();

var Year_id4 = "bedehkar3_" + parts[1];
//var val2 = $('#datepickerFromEn_Eq12323_' + parts[1]).val();
var Monthid = "bedehkar_" + parts[1];
var inputString = $("#" + Monthid).val().replace(/,/g, '') || 0;;
var inputString2 = $("#bestankar_" + parts[1]).val().replace(/,/g, '') || 0;;

obj.count = inputString2;
obj.vahd = $("#" + Year_id4).val().replace(/,/g, '') || 0;;
//obj.Title = ;
obj.FK_asliasnad = resp1;
obj.megdar = inputString;  // بدون

    var inputString200 = $("#bedehkar30_" + parts[1]).val().replace(/,/g, '') || 0;;
    var bytaghfif = $("#bedehkar31_" + parts[1]).val().replace(/,/g, '') || 0;;
    var arzish = $("#bedehkar32_" + parts[1]).val().replace(/,/g, '') || 0;;
    var nahaei = $("#bedehkar33_" + parts[1]).val().replace(/,/g, '') || 0;;
    obj.taghfif = inputString200;
    obj.bytaghfif = bytaghfif;
    obj.arzish = arzish;
    obj.nahaei = nahaei;




Filters.push(obj);
}
});
    $('.installmentAmountInputManually3000').each(function (i, obj) {
        console.log("djkjsdkl");
        let count = 0;

        if ($(this).attr("id").split("_").length == 2) {
            var obj = new Object();

            count++;
            console.log($(this).attr("id"));

            //console.log($("#select_peyman1").val());
            //console.log($("#miladi_date2").val());
            var parts = $(this).attr("id").split("_"); // Corrected line                    var inputString = $(this).val();

            //var parts = id.split("_");


            //obj.Creditorcredit = $("#bestankar").val();
            //obj.Debtorcredit = $("#bedehkar").val();
            var Year_id456 = "bedehkar3045556_" + parts[1];
            obj.taghfif = parseFloat($('#' + Year_id456).val().replace(/,/g, '')) || 0;
            obj.FK_asliasnad = resp1;
            var Year_id45688 = "bedehkar300000_" + parts[1];

            obj.sharhtaghfif = $('#' + Year_id45688).val()

            var Year_id4568833334 = "creditline2wt345_" + parts[1];
            obj.FK_nameshr = $('#' + Year_id4568833334).val()

            Filters.push(obj);







        }
    });
    $('.installmentAmountInputManually302').each(function (i, obj) {
        console.log("djkjsdkl");
        let count = 0;

        if ($(this).attr("id").split("_").length == 2) {
            var obj = new Object();

            count++;
            console.log($(this).attr("id"));

            //console.log($("#select_peyman1").val());
            //console.log($("#miladi_date2").val());
            var parts = $(this).attr("id").split("_"); // Corrected line                    var inputString = $(this).val();

            //var parts = id.split("_");


            //obj.Creditorcredit = $("#bestankar").val();
            //obj.Debtorcredit = $("#bedehkar").val();
            var Year_id456 = "bedehkar323577865_" + parts[1];
            obj.arzish = parseFloat($('#' + Year_id456).val().replace(/,/g, '')) || 0;
            obj.FK_asliasnad = resp1;
            var Year_id456788 = "bedehkar300000000_" + parts[1];
            obj.sharharzesh = $('#' + Year_id456788).val()
            var Year_id4568833334 = "creditline2w345_" + parts[1];

            obj.FK_nameshr = $('#' + Year_id4568833334).val()
            Filters.push(obj);










        }
    });
sendAjax12('@Url.Action("creatfktitlfkasnad", "FinancialDocuments", new { area = "ManagementAccounting" })', Filters);
                }




















        },
        error: function (resp1) {
        console.log(resp1);
        console.log("Nokeye");
        $("#loading-message").css("display", "none");
        Toast.fire({
        icon: 'success',
        background: '#35434A',
        color: 'white',
        title: "با خطا مواجه شده است ، لطفا بعدا تلاش فرمایید"
        }).then(() => {
        $("#loading-message").css("display", "none");
        });
        }
        });
    }

        function sendAjax12(url, data) {
        console.log(url);

        $.ajax({
        beforeSend: function () {
        document.getElementById("loading-message").style.display = "block";
        },
        url: url,
        data: { Filters: data },
        type: "POST",
        dataType: "html",
        success: function (resp) {
        console.log(resp);
        document.getElementById("loading-message").style.display = "none";
        if (resp == "True") {
        alert("با موفقیت ثبت شد");
        location.reload();
        }

        else {
        alert("مبلغ های های بستانکارهای شما بیش از حد مجاز در قرار داد است ")
        }
        },
        error: function (resp) {
        document.getElementById("loading-message").style.display = "none";
        alert("دچار مشکل شده است ");
        console.log(resp);
        }
        });
    }








    function confirmFinancialSummary456() {
        console.log("confirmFinancialSummary2");

        Swal.fire({
            title: 'فاکتور شما تراز نیست',
            icon: 'question',
            //showCancelButton: true,
            background: '/*rgb(29,40,52*/)',
            color: '/*white*/',
            confirmButtonColor: '',
            cancelButtonColor: '',
            confirmButtonText: '',
            cancelButtonText: ''
        }).then((result) => {
            if (result.isConfirmed) {
                confirmFinancialSummary();

            }
        });
    }
    function showSimpleAlert() {
        Swal.fire({
            title: 'مبلغ نهایی فاکتور با مبلغ پرداخت شده برابر نیست',
            background: 'rgb(29,40,52)'
        });
    }
    function showSimpleAlerttarigh() {
        Swal.fire({
            title: 'لطفا تاریخ فاکتور را پرکنید ',
            background: 'rgb(29,40,52)'
        });
    }
    function showSimpleAlertsanad() {
        Swal.fire({
            title: 'لطفا شماره سند مالی را تعیین نمایید ',
            background: 'rgb(29,40,52)'
        });
    }
    function showSimpleAlertsharh() {
        Swal.fire({
            title: 'لطفا شرح  مختصر فاکتور را پرکنید ',
            background: 'rgb(29,40,52)'
        });
    }
    function showSimpleAlertshomarehFACTOR() {
        Swal.fire({
            title: 'لطفا شماره فاکتور را پرکنید ',
            background: 'rgb(29,40,52)'
        });
    }
</script>
