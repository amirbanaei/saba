
@using SaabWebProject.Models.Classes;
@using SaabWebProject.Models.DomainModels;

@{
    ViewBag.Title = "سال مالی";
    Layout = "~/Views/Shared/_Layout.cshtml";
    SaabEntities db = new SaabEntities();

}
<link href="~/MyStyles/Tablestyle.css" rel="stylesheet" />

<link href="~/Assets/plugins/select-picker/dist/picker.min.css" rel="stylesheet" />

<script src="~/Assets/js/jalalidatepicker.min.js"></script>
<link href="~/Assets/css/jalalidatepicker.min.css" rel="stylesheet" />
<script src="~/Assets/js/jalaali.min.js"></script>
<link href="~/MyStyles/jalalidatepicker.css" rel="stylesheet" />

<link href="~/MyStyles/MyStyle.css" rel="stylesheet" />
<link href="~/MyStyles/Tablestyle.css" rel="stylesheet" />

<link href="~/MyStyles/MyStyle.css" rel="stylesheet" />
<link href="~/MyStyles/Tablestyle.css" rel="stylesheet" />
<div class="row" style="margin-top:10px">
    <div class="col-xl-2">
        <label class="form-label">  سال</label>
        @*<select class="form-select " id="sanad">
            <option>انتخاب کنید</option>
            @for (int I = 1; I <= 10; I++)
            {
                <option value="@I">@I</option>

            }

        </select>*@
        @{

            var find = db.dbAsnadsalvorodi.Where(p => p.year != null).FirstOrDefault();


        }

        @if (find == null)
        {
            <input class="form-control" id="marahelsabt" @*value="@Model.id.id2"*@ @*disabled*@ />

        }
        else
        {
            <input class="form-control" id="marahelsabt" @*value="@Model.id.id2"*@ @*disabled*@ value="@find.year" />

        }

    </div>
    <div class="col-xl-1">
        <button class="btn form-control buttonsabt" id="sabtcountmarahel">
            <i class="bi bi-clipboard-check fa-fw me-1 VecGeneral"></i>
            <span class="bSabt-text">ثبت</span>
        </button>
    </div>
</div>
<script src="~/Assets/plugins/select-picker/dist/picker.min.js"></script>
<script src="~/Scripts/jquery-ui.js"></script>
<script src="~/Assets/plugins/BootstrapPersianDateTimePicker/dist/jquery.md.bootstrap.datetimepicker.js"></script>
<script src="~/Scripts/jquery-3.3.1.js" type="text/javascript"></script>
<script src="~/Assets/plugins/select-picker/dist/picker.min.js"></script>
<script>
    $("#sabtcountmarahel").click(function () {
    //var PeymanArr = [];
    //$.each($("#select_peymancitys").val().toString().split(","), function (index, item) {
    //    if (item !== "" && item !== 'Select') {
    //        PeymanArr.push(item);
    //    }
    //});

        //var createmarhaleh1 = $("#createmarhaleh1").val();
        //var taeed = $("#taeed").val();
        var marahelsabt = $("#marahelsabt").val();

      if ($("#marahelsabt").val() === "0") {
        alert("لطفا ماه را انتخاب کنید");
        return;
        } else if ($("#marahelsabt").val() === "0") {
        alert("لطفا سال را انتخاب کنید");
        return;
        }
    //    console.log(PeymanArr);
    var formData = new FormData();
        //PeymanArr.forEach((item) => {
        //    formData.append("FKcity[]", item); // ارسال هر عنصر به صورت جداگانه
        //});
        formData.append("year", marahelsabt);


    // اضافه کردن فایل‌ها
    //    fd.forEach((value, key) => {
    //    formData.append(key, value);
    //});
        console.log(formData);

    $.ajax({
                  url: "@Url.Action("creatviewsalmahi", "FinancialDocuments", new { area = "ManagementAccounting" })",
        type: "POST",
        processData: false,
        contentType: false,
        data: formData,
        success: function (res) {
            if (res == "True") {
                alert("با موفقیت ثبت شد ");
                location.reload();
            }
            console.log(res);
        },
        error: function (res) {
            console.log(res);
            Toast.fire({
                icon: 'error',
                background: '#35434A',
                color: 'white',
                title: "عملیات با خطا مواجه شد"
            });
        }
    });
});

</script>
