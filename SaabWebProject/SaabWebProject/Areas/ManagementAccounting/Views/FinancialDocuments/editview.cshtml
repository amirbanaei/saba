@using SaabWebProject.Models.Classes;
@using SaabWebProject.Models.DomainModels;

@{
    SaabEntities db = new SaabEntities();

    Layout = null;
}
@model SaabWebProject.Models.DomainModels.tbfkfinancial

<style>

    .modal2 {
        display: none;
        position: absolute; /* Change this from fixed */
        z-index: 1000; /* Adjust z-index as needed */
        left: 0;
        top: 0;
        width: 65%;
        height: auto;
        overflow: auto;
        background-color: rgba(0, 0, 0, 0.4);  
    }

    .modal-content2 {
        background-color: black;
        position: relative; /* Add relative positioning */
        /*  margin: 15% auto;*/
        padding: 20px;
        border: 4px solid aqua;
        border-radius: 30px;
        width: 100%;
        max-height: 100%;
        overflow-y: auto;
    }

    .modal3 {
        display: none;
        position: absolute; /* Change this from fixed */
        z-index: 1000; /* Adjust z-index as needed */
        left: 0;
        top: 0;
        width: 65%;
        height: auto;
        overflow: auto;
        background-color: rgba(0, 0, 0, 0.4);
    }

    .modal-content3 {
        background-color: black;
        position: relative; /* Add relative positioning */
        /*  margin: 15% auto;*/
        padding: 20px;
        border: 4px solid aqua;
        border-radius: 30px;
        width: 100%;
        max-height: 100%;
        overflow-y: auto;
    }

    .close {
        color: #aaa;
        float: right;
        font-size: 28px;
        font-weight: bold;
    }

        .close:hover,
        .close:focus {
            color: black;
            text-decoration: none;
            cursor: pointer;
        }

    .fade-in {
        animation: fadeIn 1s ease-in-out; /* Adjust duration and timing function as needed */
    }
</style>



<div class="row">

    <div class="col-xl-2">
        <label class="form-label">تاریخ </label>
        @*<input class="form-control" id="datepickerFrom_Eq" />
        <input class="form-control" type="hidden" id="datepickerFromEn_Eq" value="@Model.Starttime" />*@

        @*------------------------------------------------[1402/09/07]-|KH|-*@
        <input data-jdp jdp-btn-close data-jdp-miladi-input="datepickerFromEn_Eq" id="datepickerFrom_Eq" class="form-control pdate" name="datepickerFrom_shw1" >

       
        <input id="datepickerFromEn_Eq" value="@((Model.DataDocument != null) ? Model.DataDocument.Value.ToString("MM/dd/yyyy") : null)" type="hidden" />
        @*-------------------------------------------------------------|KH|-*@
    </div>
    @*<div class="col-xl-2">


        <label class="form-label"> تاریخ  </label>

        <input class="form-control bestankar-input" id="Creditor" value="@Model.DataDocument" />
    </div>*@


    <div class="col-xl-2">

        <label class="form-label">  شماره دبیرخانه  </label>

        <input class="form-control bedehkar-input" id="Debtore" value="@Model.numbershomar" />
    </div>
    <div class="col-xl-2">


        <label class="form-label">  شرح  </label>

        <input class="form-control bedehkar-input" id="Title2" value="@Model.Title" />
    </div>

</div>

<div class="row">
    @*<div class="col-xl-3">
            <a href="#" class="btn btn-outline-info form-control" id="Show_Etebar">
                <i class="fa fa-eye"></i>
                مشاهده اعتبار ردیف ها
            </a>
        </div>
        <div class="col-xl-2">
            <a href="#" class="btn btn-outline-info form-control">
                <i class="bi bi-download"></i>
                دانلود فایل نمونه
            </a>

        </div>
        <div class="col-xl-2">
            <a href="#" class="btn btn-outline-info form-control">
                <i class="bi bi-upload"></i>
                بارگذاری اطلاعات
            </a>

        </div>*@
    <div class="col-xl-11"></div>
    <div class="col-xl-1">
        <div class="form-group mb-3">
            <button class="btn form-control buttonsabt" id="btnCreate12">
                <i class="bi bi-clipboard-check fa-fw me-1 VecGeneral"></i>
                <span class="bSabt-text">ثبت</span>
            </button>
        </div>
    </div>
</div>
<script>
    jalaliDatepicker.startWatch({
        time: false,
        days: ["شنبه", "1شنبه", "2شنبه", "3شنبه", "4شنبه", "5شنبه", "جمعه"],
        persianDigits: true,
        autoClose: true,
        //autocomplete: true,
        showCloseBtn: true,
        showEmptyBtn: false,
        showCloseBtn: true,
        zIndex: 100000,
    });
    date = document.getElementById("datepickerFromEn_Eq").value.replace(" ", "")
    valueD = new Date(date.slice(0, 10)).toLocaleDateString('fa-IR-u-nu-latn', { year: 'numeric', month: '2-digit', day: '2-digit' })
    valueD = valueD + " " + date.slice(10, 30)
    document.getElementById("datepickerFrom_Eq").value = valueD


    //-----------------------------------------------------------------
    var inputs = document.querySelectorAll("[data-jdp-miladi-input]");
    for (var i = 0; i < inputs.length; i++) {
        inputs[i].addEventListener("jdp:change", function (e) {
            var miladiInput = document.getElementById(this.getAttribute("data-jdp-miladi-input"));
            var date = this.value.replace(' ', '').slice(0, 10).split('/');
            date = jalaali.toGregorian(parseInt(date[0]), parseInt(date[1]), parseInt(date[2]),)
            // If it was empty
            if (!this.value) {
                date = "";
            }
            // If it was only history
            else if (this.value.replace(' ', '').length < 11) {
                date = date.gy + '-' + date.gm + '-' + date.gd
            }
            // If it was the date and time
            else {
                date = date.gy + '-' + date.gm + '-' + date.gd + ' ' + this.value.replace(' ', '').slice(10, 30)
            }
            miladiInput.value = date
        });
    }
</script>
<script>
        $("#btnCreate12").click(function () {
            var Filters = [];
            var obj = new Object();

            //if ($("#bestankar").val() == 0) {
            //    alert("لطفا عنوان را انتخاب نمائید");
            //}
            //if (($("#creditline").val() == 0))
            //     {
            //    alert("لطفا عنوان را وارد  نمائید");
            //}
            //   if (($("#select_peyman1").val() == null))
            //     {
            //    alert("لطفا پیمان را انتخاب نمائید");
            //   }

            //  if (($("#datepickerFrom_Eq").val() == 0))
            //     {
            //    alert("لطفا تاریخ  را انتخاب نمائید");
            //}
           


            obj.Title = $("#Title2").val();
            obj.DataDocument = $("#datepickerFromEn_Eq").val();

            console.log($("#Title2").val());

            //      var creditorCreditValue = $("#bestankar").val();
            //      var resultString2 = creditorCreditValue.replace(/,/g, '');
            //      var debtorCreditValue = $("#bedehkar").val();
            //      var resultString = debtorCreditValue.replace(/,/g, '');

            //obj.Debtore = resultString2;
            //      obj.Debtorcredit = resultString;
            obj.numbershomar = $("#Debtore").val();
   /*         console.log($("#Debtore").val());*/

            //obj.Creditor = $("#Creditor").val();
            //console.log($("#Creditor").val());

                /*  obj.Determining_creditline_ID = $("#idd").val();*/

            obj.ID=@Model.ID;
                  Filters.push(obj);
                   console.log(Filters);
                  sendAjax11('@Url.Action("edittt", "FinancialDocuments", new { area = "ManagementAccounting" })', Filters);

              ;

        });
    function sendAjax11(url, data) {
                console.log(url);

                $.ajax({
                    beforeSend: function () {
                        document.getElementById("loading-message").style.display = "block";
                    },
                    url: url,
                    data: { tbfk: data },
                    type: "POST",
                    dataType: "html",
                    success: function (resp) {
                        console.log(resp);
                        document.getElementById("loading-message").style.display = "none";
                        if (resp == "True") {
                            alert("با موفقیت ثبت شد");
                            location.reload();

                        } else {
                            alert("مبلغ های های بستانکارهای شما بیش از حد مجاز در قرار داد است");
                        }
                    },
                    error: function (resp) {
                        document.getElementById("loading-message").style.display = "none";
                        alert("دچار مشکل شده است ");
                        console.log(resp);
                    }
                });
            }
</script>