@model SaabWebProject.Models.ViewModels.Contracts.Function.FunctionModel

@{
    Layout = null;
}
@using SaabWebProject.Models.DomainModels;

@{

    ViewBag.Title = "ثبت دستورات";
    SaabEntities db = new SaabEntities();

}

<link href="~/Assets/plugins/select-picker/dist/picker.min.css" rel="stylesheet" />

<script src="~/Assets/js/jalalidatepicker.min.js"></script>
<link href="~/Assets/css/jalalidatepicker.min.css" rel="stylesheet" />
<script src="~/Assets/js/jalaali.min.js"></script>
<link href="~/MyStyles/jalalidatepicker.css" rel="stylesheet" />

<link href="~/MyStyles/MyStyle.css" rel="stylesheet" />
<link href="~/MyStyles/Tablestyle.css" rel="stylesheet" />

<link href="~/MyStyles/MyStyle.css" rel="stylesheet" />
<div class="">
    <div class="card">
        <div class="card-body pb-2">
            <div class="row" style="margin-top:10px">
                <div class="col-xl-2">
                    <label class="form-label">
                        نام بانک                                @*<span class="red-star">*</span>*@
                    </label>
                    <input class="form-control" id="namebank" value="@Model.namebank" @*type="number" name="usr_NationalCode"*@ @*onkeyup="ValidationInput(this)"*@ />
                </div>
                <div class="col-xl-2">
                    <label class="form-label">
                        شماره حساب                            @*<span class="red-star">*</span>*@
                    </label>
                    <input class="form-control" id="hesabbank" value="@Model.hesabbank" @*type="number" name="usr_NationalCode"*@ @*onkeyup="ValidationInput(this)"*@ />
                </div>

                @*<div class="col-xl-3">
            <label class="form-label">
                انتخاب شماره حساب
            </label>

            <select class="form-select" id="shomarehhesab2">
                @foreach (var it in db.dbAsnadCheack2.Where(s => s.shomarehhesab != null).ToList())
                {
                    <option value="@it.ID">@it.shomarehhesab</option>

                }

            </select>
        </div>


        <div class="col-xl-3">
            <label class="form-label">
                انتخاب سریال چک
            </label>

            <select class="form-select" id="SJOMERH_seryal2">
                @foreach (var it in db.dbAsnadCheack3.Where(s => s.SJOMERH_seryal != null).ToList())
                {
                    <option value="@it.ID">@it.SJOMERH_seryal</option>

                }

            </select>
        </div>*@
                <div class="col-xl-1">
                    <label class="form-label"> تاریخ صدور </label>
                    <div class="col-xl-12">
                        @*------------------------------------------------[1402/10/14]-|KH|-*@

                        <input data-jdp jdp-btn-close data-jdp-miladi-input="datapick" id="datapickee" class="form-control pdate" name="datepickerFrom_shw1">
                        <input id="datapick" value="@Model.timeroz.ToString("MM/dd/yyyy")" type="hidden" />

                        @*<input data-jdp jdp-btn-close data-jdp-miladi-input="datepickerFromEn_Eq12" id="datepickerFrom_Eq12" class="form-control">
                <input id="datepickerFromEn_Eq12" type="hidden" value="@Model.timeroz.ToString("MM/dd/yyyy")" />*@
                        @*-------------------------------------------------------------|KH|-*@
                    </div>
                </div>

                <div class="col-xl-2">
                    <label class="form-label"> تاریخ چک </label>
                    <div class="col-xl-12">

                        <input data-jdp jdp-btn-close data-jdp-miladi-input="datapick2" id="datapickee2" class="form-control pdate" name="datepickerFrom_shw1">
                        <input id="datapick2" @*value="null"*@ type="hidden" />

                        @*------------------------------------------------[1402/10/14]-|KH|-
                <input data-jdp jdp-btn-close data-jdp-miladi-input="datepickerFromEn_Eq123" id="datepickerFrom_Eq123" class="form-control">
                <input id="datepickerFromEn_Eq123" type="hidden" />*@
                        @*-------------------------------------------------------------|KH|-*@
                    </div>
                </div>

                <div class="col-xl-4">
                    <label class="form-label"> کاربران حقیقی </label>
                    <select class="form-select select_peyman" id="usr" name="select_peyman">
                        <option value="-1" hidden>انتخاب کنید</option>
                        @foreach (var item in db.tbUsers.ToList())
                        {
                            <option value="@item.usr_ID">@item.FullName</option>
                        }
                    </select>
                </div>

                <div class="col-xl-4">
                    <label class="form-label"> کاربران  حقوقی </label>
                    <select class="form-select select_peyman" id="company" name="select_peyman">
                        <option value="-1" hidden>انتخاب کنید</option>
                        @foreach (var item in db.tbCompanies.ToList())
                        {
                            <option value="@item.ID">@item.CompanyName</option>
                        }
                    </select>
                </div>
               
            </div>
            <div class="row">
                <div class="col-xl-12">
                    <label class="form-label">
                        شرح چک                             @*<span class="red-star">*</span>*@
                    </label>
                    <input class="form-control" id="sharhcheak" @*value="@Model.namebank"*@ @*type="number" name="usr_NationalCode"*@ @*onkeyup="ValidationInput(this)"*@ />
                </div>
            </div>
            <div class="row" style="margin-bottom:10px;">
                <div class="col-xl-11"></div>

                @*<div class="col-xl-1">
                        <a href="#" class=" btn form-control buttonGeneral" id="dwn" onclick="DownloadExcel23() ">
                            <i class="bi bi-download fa-fw me-1 VecGeneral"></i>
                            <span class="bGeneral-text">دانلود فایل نمونه</span>
                        </a>
                    </div>*@
                <div class="col-xl-1" id="submitdiv">
                    <button class="btn buttonsabt form-control" id="btnCreate3bank">
                        <i class="bi bi-clipboard-check fa-fw me-1 VecGeneral"></i>
                        <span class="bSabt-text">ویرایش</span>
                    </button>

                </div>
            </div>

        </div>

        <div class="card-arrow">
            <div class="card-arrow-top-left"></div>
            <div class="card-arrow-top-right"></div>
            <div class="card-arrow-bottom-left"></div>
            <div class="card-arrow-bottom-right"></div>
        </div>
    </div>
</div>
<script src="~/Scripts/jquery-ui.js"></script>
<script src="~/Assets/js/jquery3.6.0.js"></script>
<script src="~/Assets/plugins/select-picker/dist/picker.min.js"></script>
<script>


    //for (var i = 0; i < inputs.length; i++) {
    //    inputs[i].addEventListener("jdp:change", function (e) {
    //        var miladiInput = document.getElementById(this.getAttribute("data-jdp-miladi-input"));
    //        var date = this.value.replace(' ', '').slice(0, 10).split('/');
    //        date = jalaali.toGregorian(parseInt(date[0]), parseInt(date[1]), parseInt(date[2]),)
    //        // If it was empty
    //        if (!this.value) {
    //            date = "";
    //        }
    //        // If it was only history
    //        else if (this.value.replace(' ', '').length < 11) {
    //            date = date.gy + '-' + date.gm + '-' + date.gd
    //        }
    //        // If it was the date and time
    //        else {
    //            date = date.gy + '-' + date.gm + '-' + date.gd + ' ' + this.value.replace(' ', '').slice(10, 30)
    //        }
    //        miladiInput.value = date
    //        var weekDay = getDayOfWeek(document.querySelector("#datapick").value.slice(0, 10))
    //        document.querySelector("#weekDay").value = weekDay
    //    });
    //}
    //for (var i = 0; i < inputs.length; i++) {
    //    inputs[i].addEventListener("jdp:change", function (e) {
    //        var miladiInput = document.getElementById(this.getAttribute("data-jdp-miladi-input"));
    //        var date = this.value.replace(' ', '').slice(0, 10).split('/');
    //        date = jalaali.toGregorian(parseInt(date[0]), parseInt(date[1]), parseInt(date[2]),)
    //        // If it was empty
    //        if (!this.value) {
    //            date = "";
    //        }
    //        // If it was only history
    //        else if (this.value.replace(' ', '').length < 11) {
    //            date = date.gy + '-' + date.gm + '-' + date.gd
    //        }
    //        // If it was the date and time
    //        else {
    //            date = date.gy + '-' + date.gm + '-' + date.gd + ' ' + this.value.replace(' ', '').slice(10, 30)
    //        }
    //        miladiInput.value = date
    //        var weekDay = getDayOfWeek(document.querySelector("#datapick2").value.slice(0, 10))
    //        document.querySelector("#weekDay").value = weekDay
    //    });
    //}
    //jalaliDatepicker.startWatch({
    //    time: true,
    //    days: ["شنبه", "1شنبه", "2شنبه", "3شنبه", "4شنبه", "5شنبه", "جمعه"],
    //    persianDigits: true,
    //    autoClose: true,
    //    //autocomplete: true,
    //    showCloseBtn: true,
    //    showEmptyBtn: false,
    //    showCloseBtn: true,
    //    zIndex: 100000,
    //});
    //function reinitializeDatepickers() {
    //    var inputs = document.querySelectorAll("[data-jdp-miladi-input]");
    //    console.log(inputs.length);

    //    for (var i = 0; i < inputs.length; i++) {
    //        inputs[i].addEventListener("jdp:change", function (e) {
    //            var miladiInput = document.getElementById(this.getAttribute("data-jdp-miladi-input"));
    //            var date = this.value.replace(' ', '').slice(0, 10).split('/');
    //            date = jalaali.toGregorian(parseInt(date[0]), parseInt(date[1]), parseInt(date[2]));

    //            if (!this.value) {
    //                date = "";
    //            } else if (this.value.replace(' ', '').length < 11) {
    //                date = date.gy + '-' + date.gm + '-' + date.gd;
    //            } else {
    //                date = date.gy + '-' + date.gm + '-' + date.gd + ' ' + this.value.replace(' ', '').slice(10, 30);
    //            }
    //            miladiInput.value = date;
    //        });
    //    }
    //}

    //-----------------------------------------------------------------
    //var inputs = document.querySelectorAll("[data-jdp-miladi-input]");
    //for (var i = 0; i < inputs.length; i++) {
    //    console.log(inputs.length);

    //    inputs[i].addEventListener("jdp:change", function (e) {
    //        var miladiInput = document.getElementById(this.getAttribute("data-jdp-miladi-input"));
    //        var date = this.value.replace(' ', '').slice(0, 10).split('/');
    //        date = jalaali.toGregorian(parseInt(date[0]), parseInt(date[1]), parseInt(date[2]),)
    //        // If it was empty
    //        if (!this.value) {
    //            date = "";
    //        }
    //        // If it was only history
    //        else if (this.value.replace(' ', '').length < 11) {
    //            date = date.gy + '-' + date.gm + '-' + date.gd
    //        }
    //        // If it was the date and time
    //        else {
    //            date = date.gy + '-' + date.gm + '-' + date.gd + ' ' + this.value.replace(' ', '').slice(10, 30)
    //        }
    //        miladiInput.value = date
    //    });
    //}
    $.each($("#usr"), function (index, item) {
        $("#usr").attr("id", "select_usr_");
        $('#select_usr_').attr("multiple", "multiple");
        $('#select_usr_').picker({ search: true });
        $('#select_usr_').on('sp-change', function () { });
    }); $.each($("#company"), function (index, item) {
        $("#company").attr("id", "select_company_");
        $('#select_company_').attr("multiple", "multiple");
        $('#select_company_').picker({ search: true });
        $('#select_company_').on('sp-change', function () { });
    });
    function reinitializeDatepickers() {
        var inputs = document.querySelectorAll("[data-jdp-miladi-input]");
        console.log(inputs.length);

        for (var i = 0; i < inputs.length; i++) {
            inputs[i].addEventListener("jdp:change", function (e) {
                var miladiInput = document.getElementById(this.getAttribute("data-jdp-miladi-input"));
                var date = this.value.replace(' ', '').slice(0, 10).split('/');
                date = jalaali.toGregorian(parseInt(date[0]), parseInt(date[1]), parseInt(date[2]));

                if (!this.value) {
                    date = "";
                } else if (this.value.replace(' ', '').length < 11) {
                    date = date.gy + '-' + date.gm + '-' + date.gd;
                } else {
                    date = date.gy + '-' + date.gm + '-' + date.gd + ' ' + this.value.replace(' ', '').slice(10, 30);
                }
                miladiInput.value = date;
            });
        }
    }
    var inputs = document.querySelectorAll("[data-jdp-miladi-input]");
    for (var i = 0; i < inputs.length; i++) {
        console.log(inputs.length);

        inputs[i].addEventListener("jdp:change", function (e) {
            var miladiInput = document.getElementById(this.getAttribute("data-jdp-miladi-input"));
            var date = this.value.replace(' ', '').slice(0, 10).split('/');
            date = jalaali.toGregorian(parseInt(date[0]), parseInt(date[1]), parseInt(date[2]),)
            // If it was empty
            if (!this.value) {
                date = "";
            }
            // If it was only history
            else if (this.value.replace(' ', '').length < 11) {
                date = date.gy + '-' + date.gm + '-' + date.gd
            }
            // If it was the date and time
            else {
                date = date.gy + '-' + date.gm + '-' + date.gd + ' ' + this.value.replace(' ', '').slice(10, 30)
            }
            miladiInput.value = date
        });
    }
    var valueD
    var date

    // convert miladi to jalali
    date = document.getElementById("datapick").value.replace(" ", "")
    console.log(date)
    valueD = new Date(date.slice(0, 10)).toLocaleDateString('fa-IR-u-nu-latn', { year: 'numeric', month: '2-digit', day: '2-digit' })
    valueD = valueD + " " + date.slice(10, 30)
    document.getElementById("datapickee").value = valueD
    date = document.getElementById("datapick2").value.replace(" ", "")
    console.log(date)
    valueD = new Date(date.slice(0, 10)).toLocaleDateString('fa-IR-u-nu-latn', { year: 'numeric', month: '2-digit', day: '2-digit' })
    valueD = valueD + " " + date.slice(10, 30)
    document.getElementById("datapickee2").value = valueD
    var inputs = document.querySelectorAll("[data-jdp-miladi-input]");
    for (var i = 0; i < inputs.length; i++) {
        console.log(inputs.length);

        inputs[i].addEventListener("jdp:change", function (e) {
            var miladiInput = document.getElementById(this.getAttribute("data-jdp-miladi-input"));
            var date = this.value.replace(' ', '').slice(0, 10).split('/');
            date = jalaali.toGregorian(parseInt(date[0]), parseInt(date[1]), parseInt(date[2]),)
            // If it was empty
            if (!this.value) {
                date = "";
            }
            // If it was only history
            else if (this.value.replace(' ', '').length < 11) {
                date = date.gy + '-' + date.gm + '-' + date.gd
            }
            // If it was the date and time
            else {
                date = date.gy + '-' + date.gm + '-' + date.gd + ' ' + this.value.replace(' ', '').slice(10, 30)
            }
            miladiInput.value = date
        });
    }
    var valueD
    var date

    // convert miladi to jalali
    date = document.getElementById("datapick").value.replace(" ", "")
    console.log(date)
    valueD = new Date(date.slice(0, 10)).toLocaleDateString('fa-IR-u-nu-latn', { year: 'numeric', month: '2-digit', day: '2-digit' })
    valueD = valueD + " " + date.slice(10, 30)
    document.getElementById("datapickee").value = valueD
    date = document.getElementById("datapick2").value.replace(" ", "")
    console.log(date)
    valueD = new Date(date.slice(0, 10)).toLocaleDateString('fa-IR-u-nu-latn', { year: 'numeric', month: '2-digit', day: '2-digit' })
    valueD = valueD + " " + date.slice(10, 30)
    document.getElementById("datapickee2").value = valueD




                    function sendAjax3(url2, fd2) {
            //var debtorCreditValue = $("#pestenka").val();
            //var resultString = debtorCreditValue.replace(/,/g, '');
            //fd2.append("Creditor", resultString);
            //var debtorCreditValue2 = $("#bdehkar").val();



            //console.log(resultString)
            //var resultString2 = debtorCreditValue2.replace(/,/g, '');
            //console.log(resultString2)
            //console.log(resultString2)

            //console.log(resultString)


                @{
                    long id = 0;
                }
                @if (Model != null)
                {
                    id = Model.ID;
                }
                @*fd2.append("FK_upload2", @id);
                var PeymanArr = [];
                $.each($("#select_peyman_").val().toString().split(","), function (index, item) {
                    if (item !== "") {
                        if (item != 'Select') {
                            PeymanArr.push(item);
                            console.log(item);
                        }
                    }
                });*@
                var usrnArr = [];
                $.each($("#select_usr_").val().toString().split(","), function (index, item) {
                    if (item !== "") {
                        if (item != 'Select') {
                            usrnArr.push(item);
                            console.log(item);
                        }
                    }
                });
                var compauinArr = [];
                        $.each($("#select_company_").val().toString().split(","), function (index, item) {
                    if (item !== "") {
                        if (item != 'Select') {
                            compauinArr.push(item);
                            console.log(item);
                        }
                    }
                });


                console.log(usrnArr);
                //console.log(compauinArr);
                //console.log(PeymanArr);
                fd2.append("fkcompain", compauinArr );
                fd2.append("fkusr", usrnArr );
              /*  fd2.append("fkPymn", PeymanArr);*/
                    fd2.append("sharhhoshdar", $("#sharhhoshdar").val());

                    fd2.append("ID", @Model.IDbank);
                    //fd2.append("count_dafaat", $("#count_dafaat").val());
                    //fd2.append("DOreh", $("#DOreh").val());
                        fd2.append("Sharh", $("#sharhcheak").val());
                    //fd2.append("typehoshdar", $("#typehoshdar").val());

                        fd2.append("datepickerFromEn_usr", $("#datapick").val());
                //fd2.append("datepickerFromEn_usrtamdid", $("#datepickerFromEn_usrtamdid").val());
                        fd2.append("datepickerFromEn_usrrepet", $("#datapick2").val());
                //fd2.append("datepickerFromEn_usrend", $("#datepickerFromEn_usrend").val());
                //fd2.append("mablaghChecked", $("#mablaghChecked").val());
                //fd2.append("shomarehnameh", $("#shomarehnameh").val());

                //console.log($("#compain").val());
                $("#loading-circle .loadingText1").text("لطفا منتظر نمایش اطلاعات بمانید ...");
                $("#loading-circle").css("display", "flex");
            $.ajax({
                url: url2,
                type: 'POST',
                data: fd2,
                processData: false,
                contentType: false,
                success: function (resp1) {
                    if (resp1 === "True") {
                        $("#loading-circle").css("display", "none"); // پنهان کردن لودینگ
                        alert("با موفقیت ثبت شد");
                        location.reload();
                    }
                    else {
                        $("#loading-circle").css("display", "none"); // پنهان کردن لودینگ
                        alert(resp1);

                    }
   //                 console.log("okeye");
   //                 console.log(resp1);
   //                 $("#loading-message").css("display", "none");



   //console.log("djkjsdkl");









                },
                error: function (resp1) {
                    console.log(resp1);
                    console.log("Nokeye");
                    $("#loading-message").css("display", "none");
                    Toast.fire({
                        icon: 'success',
                        background: '#35434A',
                        color: 'white',
                        title: "با خطا مواجه شده است ، لطفا بعدا تلاش فرمایید"
                    }).then(() => {
                        $("#loading-message").css("display", "none");
                    });
                }
            });
    }
    var fd4 = new FormData();

            $("#btnCreate3bank").click(function () {

         //       console.log($("#year").val());
                sendAjax3('@Url.Action("createcheack4", "FinancialDocuments", new { area = "ManagementAccounting" })', fd4);

});
</script>