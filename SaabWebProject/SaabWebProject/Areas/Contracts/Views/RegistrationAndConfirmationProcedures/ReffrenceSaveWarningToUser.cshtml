@model SaabWebProject.Models.DomainModels.tbReffrenceSaveLevel

@if (Model.ID != 0)
{
    <a href="#" data-bs-toggle="dropdown" data-bs-display="static" class="menu-link">
        <div class="menu-icon"><i class="bi bi-dash-circle nav-icon" style="color:green;"></i></div>
        <div class="menu-badge bg-theme"></div>
    </a>
    <div class="dropdown-menu dropdown-menu-end mt-1 w-300px fs-11px pt-1">
        <h6 class="dropdown-header fs-10px mb-1">یادآور ثیت مولفه های عملکردی</h6>
        <div class="dropdown-divid
             er mt-1"></div> 

        <a href="@Url.Action("Index", "Functions", new { area = "Contracts" })" class="d-flex align-items-center py-10px dropdown-item text-wrap"  id="enheraf_az_meyar">
            <div class="fs-20px">
                <i class="bi bi-bullseye text-theme"></i>
            </div>
            <div class="flex-1 flex-wrap ps-3">
                <div class="mb-1 text-white">دسترسی برای ثبت عملکرد شما باز شده است</div>
                <div class="small">از @Model.StartDaysInMonth ام به مدت @Model.DeadLine روز</div>
            </div>
            <div class="ps-2 fs-16px">
                <i class="bi bi-chevron-left"></i>
            </div>
        </a>
    </div>

}
else
{

}
