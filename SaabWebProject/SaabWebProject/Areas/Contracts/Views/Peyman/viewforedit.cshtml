
@{
    ViewBag.Title = "ویرایش";
    Layout = null;
}
<link href="~/MyStyles/buttonstyle.css" rel="stylesheet" />
@model SaabWebProject.Models.DomainModels.tblink_moalfe_city_valu
<link href="~/MyStyles/inputstyleandformstyle.css" rel="stylesheet" />

<script src="~/Assets/js/jalalidatepicker.min.js"></script>
<link href="~/Assets/css/jalalidatepicker.min.css" rel="stylesheet" />
<script src="~/Assets/js/jalaali.min.js"></script>
<link href="~/MyStyles/jalalidatepicker.css" rel="stylesheet" />
<div class="row">
    <div class="col-xl-5"></div>
    <div class="col-xl-2">
        <label class="form-label"> مقدار </label>
        <input class="form-control bestankar-input" id="bestankar" value="@Model.value"  />
    </div>

    <div class="col-xl-5"></div>

    <script>
        function formatCurrency(input) {
            // حذف همه کاراکترهای غیر از اعداد و "." (جداکننده اعشاری)
            let value = input.value.replace(/[^\d.]/g, '');

            // تبدیل مقدار به عدد اعشاری
            let number = parseFloat(value);

            // اگر عدد معتبر باشد، آن را به صورت سه رقمی جداکنده کنید
            if (!isNaN(number)) {
                let formattedValue = number.toLocaleString('en-US', { minimumFractionDigits: 0, maximumFractionDigits: 0 });
                // قرار دادن مقدار فرمت شده در ورودی
                input.value = formattedValue;
            }
        }
    </script>


</div>
<input style="display:none" value="@Model.ID" id="idd" />
<div class="row">
    @*<div class="col-xl-3">
            <a href="#" class="btn btn-outline-info form-control" id="Show_Etebar">
                <i class="fa fa-eye"></i>
                مشاهده اعتبار ردیف ها
            </a>
        </div>
        <div class="col-xl-2">
            <a href="#" class="btn btn-outline-info form-control">
                <i class="bi bi-download"></i>
                دانلود فایل نمونه
            </a>

        </div>
        <div class="col-xl-2">
            <a href="#" class="btn btn-outline-info form-control">
                <i class="bi bi-upload"></i>
                بارگذاری اطلاعات
            </a>

        </div>*@
    <div class="col-xl-11"></div>
    <div class="col-xl-1">
        <div class="form-group mb-3">
            <button class="btn btn-outline-success form-control buttonsabt" id="btnCreate12">
                <i class="bi bi-clipboard-check fa-fw me-1 VecGeneral"></i>
                <span class="bSabt-text">ثبت</span>
            </button>
        </div>
    </div>
</div>

<script>
        $("#btnCreate12").click(function () {

            var Filters = [];


            var obj = new Object();
            console.log($("#bestankar").val());
                  var creditorCreditValue = $("#bestankar").val();
                  var resultString2 = creditorCreditValue.replace(/,/g, '');
            console.log(resultString2);

            obj.Creditorcredit = resultString2;

            obj.Determining_creditline_ID = $("#idd").val();
            console.log($("#idd").val());

            var obj2 = {
                Creditorcredit: resultString2,
                Determining_creditline_ID: $("#idd").val()
};

sendAjax('@Url.Action("edit", "Peyman", new { area = "Contracts" })', obj2);

                  @*Filters.push(obj);
                   console.log(Filters);
                  sendAjax('@Url.Action("edit", "Peyman", new { area = "Contracts" })', obj);*@




        });
        function sendAjax(url, data) {
            console.log(url);
            console.log(data);


                $.ajax({
                    beforeSend: function () {
                        document.getElementById("loading-message").style.display = "block";
                    },
                    url: url,
                    data: data, // تغییر این قسمت                    type: "POST",
                    dataType: "html",
                    success: function (resp) {
                        console.log(resp);
                        document.getElementById("loading-message").style.display = "none";
                        if (resp == "True") {
                            alert("با موفقیت ثبت شد");
                            location.reload();

                        } else {
                            alert("مبلغ های های بستانکارهای شما بیش از حد مجاز در قرار داد است");
                        }
                    },
                    error: function (resp) {
                        document.getElementById("loading-message").style.display = "none";
                        alert("دچار مشکل شده است ");
                        console.log(resp);
                    }
                });
            }
</script>
