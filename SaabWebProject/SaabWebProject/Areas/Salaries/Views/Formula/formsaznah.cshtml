@using SaabWebProject.Models.DomainModels;

@{
    SaabEntities db = new SaabEntities();
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>viewTools</title>

</head>
<body>
    <style>



        .tab-content {
            height: fit-content !important;
            padding-right: 2px !important;
            padding-left: 2px !important;
        }

        @@media print {
            #luckysheet {
                display: none;
            }
        }

        .modal_table {
            direction: rtl !important;
        }

        .hljs-container {
            height: fit-content !important;
            /*            overflow: hidden*/
        }

        .tab-content {
            position: relative;
            margin: 0;
            padding: 10px;
            height: 89vh;
            overflow: hidden
        }

        .topBox {
            position: relative
        }

        #luckysheet {
            padding: 2px;
            position: relative !important;
            height: 541.781px !important;
            width: 1195.36px !important;
            left: 0px;
            /*top: 0px;*/
            direction: ltr;
        }

        .luckysheet-cols-menu {
            direction: ltr;
        }

        .luckysheet-modal-dialog-slider {
            height: 607px;
            top: 67px;
            right: 75px;
            width: 310px;
            padding: 2px;
            border-radius: 10px;
            overflow: hidden;
        }

        .luckysheet {
            height: 502.781px !important;
            width: 1195.36px !important;
        }

        .luckysheet_info_detail {
            display: none !important;
        }

        .luckysheet-functionbox-cell-input {
            color: #000 !important
        }

        #luckysheet-modal-dialog-slider-imageCtrl {
            color: #000;
            top: 66px;
            right: 76px;
            width: 280px;
            border: 2px solid #16525c;
            border-radius: 15px;
            backdrop-filter: blur(10px);
        }

        #luckysheet-rich-text-editor {
            color: #000;
        }

        #luckysheet-modal-dialog-slider-imageCtrl .luckysheet-modal-dialog-slider-content .luckysheet-modal-dialog-slider-content {
            background-color: #ffffff94 !important;
        }
        /**/
        #luckysheet-modal-dialog-slider-protection {
            color: #000;
            top: 66px;
            right: 76px;
            width: 315px;
            border: 2px solid #16525c;
            border-radius: 15px;
            backdrop-filter: blur(10px);
            padding: 4px
        }

            #luckysheet-modal-dialog-slider-protection .luckysheet-modal-dialog-slider-content {
                background: unset !important;
            }

        .luckysheet-modal-dialog-slider-content input {
            margin-bottom: 0px !important;
            margin-left: 6px;
        }
        /**/
        #luckysheet-insertLink-dialog {
            user-select: none;
            border-radius: 15px;
            backdrop-filter: blur(10px);
            background: #ffffff29;
            border: 2px solid #16525c;
        }

            #luckysheet-insertLink-dialog .box-item label {
                width: 100px !important;
            }

        div#luckysheet-insertLink-dialog div {
            background: #fffcfc00;
        }
        /*----------------------------------*/
        input.luckysheet-mousedown-cancel {
            border-radius: 5px !important;
            border: 1px solid !important;
        }

        .border-item input {
            border-radius: 5px !important;
            border: 1px solid !important;
        }
        /*----------------------------------*/
        #luckysheet-rightclick-menu {
            top: 65px !important;
        }

        .luckysheet-modal-dialog, .luckysheet-cols-menu {
            background: #ffffff94 !important;
            backdrop-filter: blur(4px) !important;
            border-radius: 8px !important;
        }
        /*----------------------------------*/
        #luckysheet-hyperlink-overshow {
            color: #000 !important;
        }
        /*----------------------------------*/
        .modal-dialog.modal-lg {
            max-width: 99% !important;
        }

        #big_modal_body table {
            width: 100% !important;
        }
        /*----------------------------------*/
        .luckysheet-cols-menuitem[itemvalue="vertical"] {
            display: none;
        }

        .tableModal td {
            overflow: hidden
        }
        /*----------------------------------*/
    </style>
    <style>
        . <style >
        .tab-content {
            /*                    height: !important;*/
            padding-right: 2px !important;
            padding-left: 2px !important;
        }

        @@media print {
            #luckysheet {
                display: none;
            }
        }

        .modal_table {
            direction: rtl !important;
        }

        .hljs-container {
            overflow: hidden
        }

        .tab-content {
            position: relative;
            margin: 0;
            padding: 10px;
            height: 89vh;
            overflow: hidden
        }

        .topBox {
            position: relative
        }

        #luckysheet {
            padding: 2px;
            position: absolute;
            height: 541.781px !important;
            width: 1195.36px !important;
            left: 0px;
            /*top: 0px;*/
            direction: ltr;
        }

        .luckysheet-cols-menu {
            direction: ltr;
        }

        .luckysheet-modal-dialog-slider {
            height: 607px;
            top: 67px;
            right: 75px;
            width: 310px;
            padding: 2px;
            border-radius: 10px;
            overflow: hidden;
        }

        .luckysheet {
            height: 502.781px !important;
            width: 1195.36px !important;
        }

        .luckysheet_info_detail {
            display: none !important;
        }

        .luckysheet-functionbox-cell-input {
            color: #000 !important
        }

        #luckysheet-modal-dialog-slider-imageCtrl {
            color: #000;
            top: 66px;
            right: 76px;
            width: 280px;
            border: 2px solid #16525c;
            border-radius: 15px;
            backdrop-filter: blur(10px);
        }

        #luckysheet-rich-text-editor {
            color: #000;
        }

        #luckysheet-modal-dialog-slider-imageCtrl .luckysheet-modal-dialog-slider-content .luckysheet-modal-dialog-slider-content {
            background-color: #ffffff94 !important;
        }
        /**/
        #luckysheet-modal-dialog-slider-protection {
            color: #000;
            top: 66px;
            right: 76px;
            width: 315px;
            border: 2px solid #16525c;
            border-radius: 15px;
            backdrop-filter: blur(10px);
            padding: 4px
        }

            #luckysheet-modal-dialog-slider-protection .luckysheet-modal-dialog-slider-content {
                background: unset !important;
            }

        .luckysheet-modal-dialog-slider-content input {
            margin-bottom: 0px !important;
            margin-left: 6px;
        }
        /**/
        #luckysheet-insertLink-dialog {
            user-select: none;
            border-radius: 15px;
            backdrop-filter: blur(10px);
            background: #ffffff29;
            border: 2px solid #16525c;
        }

            #luckysheet-insertLink-dialog .box-item label {
                width: 100px !important;
            }

        div#luckysheet-insertLink-dialog div {
            background: #fffcfc00;
        }
        /*----------------------------------*/
        input.luckysheet-mousedown-cancel {
            border-radius: 5px !important;
            border: 1px solid !important;
        }

        .border-item input {
            border-radius: 5px !important;
            border: 1px solid !important;
        }
        /*----------------------------------*/
        #luckysheet-rightclick-menu {
            top: 65px !important;
        }

        .luckysheet-modal-dialog, .luckysheet-cols-menu {
            background: #ffffff94 !important;
            backdrop-filter: blur(4px) !important;
            border-radius: 8px !important;
        }
        /*----------------------------------*/
        #luckysheet-hyperlink-overshow {
            color: #000 !important;
        }
        /*----------------------------------*/
        .modal-dialog.modal-lg {
            max-width: 99% !important;
        }

        #big_modal_body table {
            width: 100% !important;
        }
        /*----------------------------------*/
        .luckysheet-cols-menuitem[itemvalue="vertical"] {
            display: none;
        }

        .tableModal td {
            overflow: hidden
        }
        /*----------------------------------*/
    </style>
    <style>
        .tab-content {
            /*                    height: !important;*/
            padding-right: 2px !important;
            padding-left: 2px !important;
        }

        @@media print {
            #luckysheet {
                display: none;
            }
        }

        .modal_table {
            direction: rtl !important;
        }

        .hljs-container {
            overflow: hidden
        }

        .tab-content {
            position: relative;
            margin: 0;
            padding: 10px;
            height: 89vh;
            overflow: hidden
        }

        .topBox {
            position: relative
        }

        #luckysheet {
            padding: 2px;
            position: absolute;
            height: 541.781px !important;
            width: 1195.36px !important;
            left: 0px;
            /*top: 0px;*/
            direction: ltr;
        }

        .luckysheet-cols-menu {
            direction: ltr;
        }

        .luckysheet-modal-dialog-slider {
            height: 607px;
            top: 67px;
            right: 75px;
            width: 310px;
            padding: 2px;
            border-radius: 10px;
            overflow: hidden;
        }

        .luckysheet {
            height: 502.781px !important;
            width: 1195.36px !important;
        }

        .luckysheet_info_detail {
            display: none !important;
        }

        .luckysheet-functionbox-cell-input {
            color: #000 !important
        }

        #luckysheet-modal-dialog-slider-imageCtrl {
            color: #000;
            top: 66px;
            right: 76px;
            width: 280px;
            border: 2px solid #16525c;
            border-radius: 15px;
            backdrop-filter: blur(10px);
        }

        #luckysheet-rich-text-editor {
            color: #000;
        }

        #luckysheet-modal-dialog-slider-imageCtrl .luckysheet-modal-dialog-slider-content .luckysheet-modal-dialog-slider-content {
            background-color: #ffffff94 !important;
        }
        /**/
        #luckysheet-modal-dialog-slider-protection {
            color: #000;
            top: 66px;
            right: 76px;
            width: 315px;
            border: 2px solid #16525c;
            border-radius: 15px;
            backdrop-filter: blur(10px);
            padding: 4px
        }

            #luckysheet-modal-dialog-slider-protection .luckysheet-modal-dialog-slider-content {
                background: unset !important;
            }

        .luckysheet-modal-dialog-slider-content input {
            margin-bottom: 0px !important;
            margin-left: 6px;
        }
        /**/
        #luckysheet-insertLink-dialog {
            user-select: none;
            border-radius: 15px;
            backdrop-filter: blur(10px);
            background: #ffffff29;
            border: 2px solid #16525c;
        }

            #luckysheet-insertLink-dialog .box-item label {
                width: 100px !important;
            }

        div#luckysheet-insertLink-dialog div {
            background: #fffcfc00;
        }
        /*----------------------------------*/
        input.luckysheet-mousedown-cancel {
            border-radius: 5px !important;
            border: 1px solid !important;
        }

        .border-item input {
            border-radius: 5px !important;
            border: 1px solid !important;
        }
        /*----------------------------------*/
        #luckysheet-rightclick-menu {
            top: 65px !important;
        }

        .luckysheet-modal-dialog, .luckysheet-cols-menu {
            background: #ffffff94 !important;
            backdrop-filter: blur(4px) !important;
            border-radius: 8px !important;
        }
        /*----------------------------------*/
        #luckysheet-hyperlink-overshow {
            color: #000 !important;
        }
        /*----------------------------------*/
        .modal-dialog.modal-lg {
            max-width: 99% !important;
        }

        #big_modal_body table {
            width: 100% !important;
        }
        /*----------------------------------*/
        .luckysheet-cols-menuitem[itemvalue="vertical"] {
            display: none;
        }

        .tableModal td {
            overflow: hidden
        }
        /*----------------------------------*/
    </style>

    <div class="">
        <ul class="nav nav-tabs nav-tabs-v2 ps-4 pe-4" id="pills-tab" style="margin-bottom:30px">
            <li class="nav-item me-3">
                <a class="nav-link active" id="pills-home-tab" data-bs-toggle="pill" href="#page-1">
                    <button class=" nav-btn">اول</button>
                </a>
            </li>
            <li class="nav-item me-3">
                <a class="nav-link" id="pills-profile-tab" data-bs-toggle="pill" href="#page-2">
                    <button class=" nav-btn">دوم</button>
                </a>
            </li>
            <li class="nav-item me-3">
                <a class="nav-link" id="pills-profile2-tab" data-bs-toggle="pill" href="#page-3">
                    <button class=" nav-btn">سوم</button>
                </a>
            </li>
        </ul>
    </div>
    <div class="tab-content" id="pills-tabContent">
        <div class="tab-pane fade show active" id="page-1">


            <h3 class="mb-4">🧾 ابزار گزارش‌ساز با JOIN خودکار</h3>

            <div class="row mb-3">
                <div class="col-md-6">
                    <label>انتخاب جدول‌ها (تا ۳ عدد):</label>

                    <div id="tableCheckboxes" class="form-check" style="height: 250px; overflow-y: auto; border: 1px solid #ccc; padding: 10px;">
                        <!-- چک‌باکس‌ها اینجا اضافه می‌شن -->
                    </div>
                </div>
                <div class="col-md-6">
                    <label>ستون‌ها:</label>
                    <div id="columnsArea" style="height: 250px; overflow-y: auto; border: 1px solid #ccc; padding: 10px;"></div>
                </div>
            </div>

            <div class="mb-3">
                <label>فیلتر دستی (WHERE):</label>
                <input id="whereInput" class="form-control" placeholder="مثلاً Salary.tbMoalefeValueFish.mlfvlfsh_Year = 1403" />
            </div>

            <button id="generateSql" class="btn btn-success form-control buttonGeneral" style="right: 95%;">
                <i class="bi bi-box-seam fa-fw me-1 VecGeneral"></i>
                <span class="bGeneral-text"> تولید SQL با JOIN</span>
            </button>



            <hr />
            <h5>کد SQL نهایی:</h5>
            <pre id="sqlOutput" class="bg-white p-3 border rounded"></pre>
            <div class="mb-3">
                <label>نام گزارش:</label>
                <input id="namegozaresh" class="form-control" @*placeholder="مثلاً Salary.tbMoalefeValueFish.mlfvlfsh_Year = 1403"*@ />
            </div>
            <button id="save" class="btn btn-success form-control buttonGeneral" style="right: 95%;">
                <i class="bi bi-file-earmark-code fa-fw me-1 VecGeneral"></i>
                <span class="bGeneral-text">ذخیره کد</span>

            </button>




        </div>
        <div class="tab-pane fade " id="page-2">

            <div class="tab-content">
                <div class="row topBox">
                    <div class="col-xl-2">
                        <label class="form-label">تعداد سطر</label>
                        <input class="form-control" id="rowT" />
                    </div>
                    <div class="col-xl-2">
                        <label class="form-label">تعداد ستون</label>
                        <input class="form-control" id="colT" />
                    </div>

                    <div class="col-xl-1">
                        <button id="buildT" type="button" class="form-control buttonsabt">
                            <i class="bi bi-table fa-fw me-1 VecGeneral"></i>
                            <span class="bSabt-text">تولید جدول</span>
                        </button>
                    </div>
                    <div class="col-xl-1">
                        @*<button onclick="ShowDialogFile1()" class="form-control buttonGeneral">
                                <i class="bi bi-upload fa-fw me-1 VecGeneral"></i>
                                <span class="bGeneral-text"> بارگذاری اطلاعات</span>
                            </button>
                            <input id="uploadExcel" type="file" style="display:none" />*@
                    </div>
                    <div class="col-xl-5"></div>
                    <div class="col-xl-1">
                        <button id="savebtn" class="form-control buttonGeneral" data-bs-toggle="modal" data-bs-target="#modalLg">
                            <i class="bi bi-clipboard-check fa-fw me-1 VecGeneral"></i>
                            <span class="bSabt-text">ثبت</span>
                        </button>
                    </div>
                </div>
                <div id="myTable">

                </div>

            </div>



        </div>
        <div class="tab-pane fade" id="page-3">
            <select class="form-select select_peyman" id="select_peyman" name="select_peyman">
                <option hidden value="Select">انتخاب مورد</option>
                @foreach (var item in db.TBGOZARESHSAZ1_Creat.Where(s => s.NAME != null && s.QURY_SQL != null).ToList())
                {
                    <option value="@item.ID" data-sql="@item.QURY_SQL">@item.NAME</option>
                }
            </select>

            <!-- دکمه اضافه کردن تنظیمات -->
            <button type="button" class="btn btn-success mt-2 form-control buttonGeneral" id="addConfig" style="right:85%">
                <i class="bi bi-patch-plus fa-fw me-1 VecGeneral"></i>
                <span class="bGeneral-text">افزودن تنظیم</span>
            </button>

            <div id="selectorsContainer" class="mt-3"></div>

            <!-- ذخیره تنظیم -->
            <button type="button" class="btn btn-primary mt-3 form-control buttonGeneral" id="saveConfigs" style="right:95%; top:-50px;">
                <i class="bi bi-clipboard-check fa-fw me-1 VecGeneral"></i>
                <span class="bGeneral-text">ذخیره تنظیمات</span>
            </button>


        </div>
    </div>
    @*<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>*@
    <link href="~/Assets/plugins/select-picker/dist/picker.min.css" rel="stylesheet" />
    <script src="~/Assets/js/jalalidatepicker.min.js"></script>
    <link href="~/Assets/css/jalalidatepicker.min.css" rel="stylesheet" />
    <script src="~/Assets/js/jalaali.min.js"></script>
    <link href="~/MyStyles/jalalidatepicker.css" rel="stylesheet" />
    <script src="~/Scripts/jquery-3.3.1.js" type="text/javascript"></script>
    <script src="~/Assets/plugins/select-picker/dist/picker.min.js"></script>
    <script src="~/Scripts/jquery-ui.js"></script>
    <script src="~/Assets/plugins/BootstrapPersianDateTimePicker/dist/jquery.md.bootstrap.datetimepicker.js"></script>
    <script src="~/Assets/plugins/select-picker/dist/picker.min.js"></script>
    <script>
let selectedTables = [];
let selectedColumns = [];

$(function () {
    // دریافت جدول‌ها
    $.getJSON('@Url.Action("GetTables", "Formula", new { area = "Salaries" })', function (tables) {
        $.each(tables, function (i, name) {
            $('#tableCheckboxes').append(`
                <div class="form-check">
                    <input type="checkbox" class="form-check-input table-check" value="${name}" id="table_${i}">
                    <label class="form-check-label" for="table_${i}">${name}</label>
                </div>
            `);
        });
    });

    // وقتی جدول انتخاب می‌شود ستون‌هایش را دریافت کن
    $(document).on('change', '.table-check', function () {
        selectedTables = $('.table-check:checked').map(function () {
            return this.value;
        }).get();

        //if (selectedTables.length > 3) {
        //    alert("حداکثر ۳ جدول قابل انتخاب است.");
        //    $(this).prop('checked', false);
        //    return;
        //}

        $('#columnsArea').empty();

        selectedTables.forEach(function (table) {
            $.getJSON('@Url.Action("GetColumns", "Formula", new { area = "Salaries" })' + '?tableName=' + table, function (cols) {
                let section = `<div class="mb-2"><strong>${table}</strong><br/>`;
                cols.forEach(col => {
                    section += `<label class="me-2">
                        <input type="checkbox" class="col-checkbox" data-table="${table}" value="${col}" />
                        ${col}
                    </label>`;
                });
                section += "</div>";
                $('#columnsArea').append(section);
            });
        });
    });

    // ارسال اطلاعات به بک‌اند و گرفتن SQL کامل
    $('#generateSql').on('click', function () {
        let cols = $('.col-checkbox:checked').map(function () {
            return {
                table: $(this).data('table'),
                column: $(this).val()
            };
        }).get();

        if (cols.length === 0 || selectedTables.length === 0) {
            alert("لطفاً جدول و ستون انتخاب کنید.");
            return;
        }

        let where = $('#whereInput').val();

        $.ajax({
            type: 'POST',
            url: '@Url.Action("GenerateSql", "Formula", new { area = "Salaries" })',
            contentType: 'application/json',
            data: JSON.stringify({
                tables: selectedTables,
                columns: cols,
                whereClause: where
            }),
            success: function (result) {
                $('#sqlOutput').text(result.sql);
            }
        });
    });
    $('#save').on('click', function () {
        let cols = $('.col-checkbox:checked').map(function () {
            return {
                table: $(this).data('table'),
                column: $(this).val()
            };
        }).get();

        if (cols.length === 0 || selectedTables.length === 0) {
            alert("لطفاً جدول و ستون انتخاب کنید.");
            return;
        }

        let Qury = $('#sqlOutput').text();
        let namegozareshQ = $('#namegozaresh').val();

        console.log(Qury);
        console.log(namegozareshQ);

        $.ajax({
            type: 'POST',
            url: '@Url.Action("creatsqlqury", "Formula", new { area = "Salaries" })',
            contentType: 'application/json',
            data: JSON.stringify({
                //tables: selectedTables,
                name: namegozareshQ,
                quyry: Qury
            }),
            success: function (result) {
                alert(result);
                location.reload();
            }
        });
    });

});
    </script>
    <script>
        //$(document).ready(function () {
        //    let row = $("#rowT").val();
        //    let col = $("#colT").val();
        //    $("#myTable").load(`/Reporter/Dashboard/TableSheetShow?row=${row}&col=${col}`);
        //});

        function ShowDialogFile1() {
            console.log("ldsaksda0");
            $("input[id='uploadExcel']").val(null);
            $("input[id='uploadExcel']").click();
        }
    </script>
    <script>
        $("#buildT").on("click", function () {
            let row = $("#rowT").val();
            let col = $("#colT").val();
            $("#myTable").load(`/Salaries/Formula/viewgozareshsazmodal2?row=${row}&col=${col}`);
        });

        //$("#savebtn").on("click", function () {
        //    let data = luckysheet.getAllSheets()
        //    console.log(data[0].data)
        //});

    </script>
    <script>
        $('#savebtn').on('click', function () {
            // دریافت تمام شیت‌ها
            let data = luckysheet.getAllSheets();
            console.log(data);

            // کار با شیت اول
            var sheet = data[0];
            var sheetData = sheet.data; // داده‌های سلول‌ها
            var config = sheet.config || {};
            var borderInfo = config.borderInfo || [];
            var mergeInfo = config.merge || {};
            var rowlen = config.rowlen || {}; // ارتفاع ردیف‌ها
            var columnlen = config.columnlen || {}; // عرض ستون‌ها

            if (!sheetData) {
                alert("دیتایی موجود نیست.");
                return;
            }

            // نقشه برای رد شدن از سلول‌های ادغام‌شده
            const skipMap = {};

            // محاسبه عرض ستون‌ها
            let totalWidth = 0;
            let colWidths = [];
            const defaultColWidth = 73; // عرض پیش‌فرض در صورتی که columnlen مقدار نداشته باشه
            const numCols = sheetData[0].length; // تعداد ستون‌ها

            // استخراج عرض ستون‌ها از config.columnlen
            for (let colIndex = 0; colIndex < numCols; colIndex++) {
                let colWidth = columnlen[colIndex] ? parseInt(columnlen[colIndex]) : defaultColWidth;
                colWidths.push(colWidth);
                totalWidth += colWidth;
            }

            // لاگ کردن عرض‌ها برای دیباگ
            console.log("Column Widths:", colWidths);
            console.log("Total Width:", totalWidth);
            console.log("Column Length Data:", columnlen);

            // شروع تولید جدول HTML
            var tableHTML = '<table class="tableModal" style="width:100%; border-collapse: collapse; direction: ltr;">';

            // تولید بدنه جدول
            sheetData.forEach(function (row, rowIndex) {
                if (!row) return;
                let rowHeight = rowlen[rowIndex] ? parseInt(rowlen[rowIndex]) : 30; // ارتفاع پیش‌فرض 30px
                tableHTML += '<tr style="height:' + rowHeight + 'px;">';

                row.forEach(function (cell, colIndex) {
                    const cellKey = `${rowIndex}_${colIndex}`;
                    if (skipMap[cellKey]) return;

                    let rowspan = 1;
                    let colspan = 1;
                    let styles = '';
                    let value = (cell && cell.v != null) ? cell.v : '';
                    let formula = (cell && cell.f) ? cell.f : ''; // فرمول
                    let rotationStyle = ''; // جدید: استایل چرخش

                    // دیباگ برای بررسی وجود bl
                    if (cell && cell.bl) {
                        console.log(`Cell [${rowIndex}, ${colIndex}] is bold:`, cell.bl);
                    }

                    // پردازش ادغام‌ها
                    const mergeKey = Object.keys(mergeInfo).find(key => {
                        const item = mergeInfo[key];
                        return item.r === rowIndex && item.c === colIndex;
                    });

                    if (mergeKey) {
                        const merge = mergeInfo[mergeKey];
                        rowspan = merge.rs;
                        colspan = merge.cs;

                        for (let r = rowIndex; r < rowIndex + rowspan; r++) {
                            for (let c = colIndex; c < colIndex + colspan; c++) {
                                if (r === rowIndex && c === colIndex) continue;
                                skipMap[`${r}_${c}`] = true;
                            }
                        }
                    }

                    // پردازش استایل‌های سلول
                    if (cell) {
                        if (cell.bg) styles += `background-color:${cell.bg};`;
                        if (cell.fc) styles += `color:${cell.fc};`;
                        if (cell.fs) styles += `font-size:${cell.fs}px;`;
                        // اعمال بولد با اولویت بالا
                        if (cell.bl) styles += 'font-weight:bold !important;';
                        if (cell.it) styles += 'font-style:italic;';
                        if (cell.un) styles += 'text-decoration:underline;';
                        // ترازبندی افقی
                        if (cell.ht) {
                            if (cell.ht === '0') styles += 'text-align:center;';
                            else if (cell.ht === '1') styles += 'text-align:left;';
                            else if (cell.ht === '2') styles += 'text-align:right;';

                        }
                        // ترازبندی عمودی
                        if (cell.vt) {
                            if (cell.vt === '0') styles += 'vertical-align:middle;';
                            else if (cell.vt === '1') styles += 'vertical-align:top;';
                            else if (cell.vt === '2') styles += 'vertical-align:bottom;';
                        }
                        // قالب‌بندی عدد
                        if (cell.ct && cell.ct.fa) {
                            if (cell.ct.fa.includes('#,##0')) {
                                if (typeof value === 'number') {
                                    value = value.toLocaleString('en-US');
                                }
                            } else if (cell.ct.fa.includes('0.00')) {
                                if (typeof value === 'number') {
                                    value = value.toFixed(2);
                                }
                            }
                        }
                        var DegR = [0, -45, 45, -180, -90, 90]
                        //================================================
                        // جدید: پردازش چرخش
                        if (cell.tr) {
                            rotationStyle += `rotate: ${DegR[cell.tr]}deg;justify-self:left;`
                            if (cell.ht === '0') {
                                rotationStyle += `rotate: ${DegR[cell.tr]}deg;justify-self:center;`
                            } else if (cell.ht === '1') {
                                rotationStyle += `rotate: ${DegR[cell.tr]}deg;justify-self:left;`
                            } else if (cell.ht === '2') {
                                rotationStyle += `rotate: ${DegR[cell.tr]} deg; justify-self:right;`
                            }

                        }
                        //================================================

                    }

                    // پردازش بردرها
                    let borderStyle = '';
                    borderInfo.forEach(function (border) {
                        if (border.range && Array.isArray(border.range)) {
                            border.range.forEach(function (rangeItem) {
                                if (
                                    rowIndex >= rangeItem.row[0] &&
                                    rowIndex <= rangeItem.row[1] &&
                                    colIndex >= rangeItem.column[0] &&
                                    colIndex <= rangeItem.column[1]
                                ) {
                                    const color = border.color || '#000';
                                    const width = 1;
                                    const solid = `${width}px solid ${color};`;

                                    switch (border.borderType) {
                                        case 'border-all':
                                            borderStyle += `border:${solid}`;
                                            break;
                                        case 'border-outside':
                                            if (rowIndex === rangeItem.row[0]) borderStyle += `border-top:${solid}`;
                                            if (rowIndex === rangeItem.row[1]) borderStyle += `border-bottom:${solid}`;
                                            if (colIndex === rangeItem.column[0]) borderStyle += `border-left:${solid}`;
                                            if (colIndex === rangeItem.column[1]) borderStyle += `border-right:${solid}`;
                                            break;
                                        case 'border-horizontal':
                                            borderStyle += `border-top:${solid} border-bottom:${solid}`;
                                            break;
                                        case 'border-vertical':
                                            borderStyle += `border-left:${solid} border-right:${solid}`;
                                            break;
                                        case 'border-left':
                                            borderStyle += `border-left:${solid}`;
                                            break;
                                        case 'border-right':
                                            borderStyle += `border-right:${solid}`;
                                            break;
                                        case 'border-top':
                                            borderStyle += `border-top:${solid}`;
                                            break;
                                        case 'border-bottom':
                                            borderStyle += `border-bottom:${solid}`;
                                            break;
                                        case 'border-none':
                                            borderStyle += 'border:none;';
                                            break;
                                    }
                                }
                            });
                        }
                    });

                    // تنظیم عرض سلول (برای سلول‌های ادغام‌شده، عرض کل را محاسبه می‌کنیم)
                    let colWidth = 0;
                    if (colspan > 1) {
                        // اگر سلول ادغام‌شده است، عرض کل ستون‌های ادغام‌شده را جمع می‌کنیم
                        for (let i = colIndex; i < colIndex + colspan; i++) {
                            colWidth += colWidths[i] || defaultColWidth;
                        }
                    } else {
                        colWidth = colWidths[colIndex] || defaultColWidth;
                    }
                    //colWidth = colWidth + 'px';

                    // تولید محتوای سلول با چرخش
                    let content = value;
                    if (rotationStyle) {
                        content = `<div style="${rotationStyle}">${value}</div>`;
                    } else {
                        content = `<div style="">${value}</div>`
                    }

                    // تولید تگ td
                    tableHTML += `<td style="width:${colWidth * 100 / totalWidth}%; padding:4px; ${styles}${borderStyle}"` +
                        (cell && cell.f ? ` data-formula="${cell.f}"` : '') +
                        (rowspan > 1 ? ` rowspan="${rowspan}"` : '') +
                        (colspan > 1 ? ` colspan="${colspan}"` : '') +
                        '>' + content + '</td>';
                });
                tableHTML += '</tr>';
            });

            tableHTML += '</table>';

            // افزودن جدول به مودال
            $('#big_modal_body').html(tableHTML);
        });
    </script>
    <script>
    let selectedGozareshId = null;
    let currentSelectOptions = [];

    $('#select_peyman').on('change', function () {
        selectedGozareshId = $(this).val();

        if (!selectedGozareshId || selectedGozareshId === 'Select') return;

        // گرفتن ستون‌ها از دیتابیس
        $.ajax({
            url: '/Salaries/Formula/GetSelectedColumns',
            type: 'GET',
            data: { id: selectedGozareshId },
            success: function (res) {
                currentSelectOptions = res;

                console.log("ستون‌های دریافتی:", currentSelectOptions);
            },
            error: function () {
                alert("خطا در بارگذاری ستون‌ها.");
            }
        });
    });


    // اضافه کردن یک سطر تنظیم
    $('#addConfig').on('click', function () {
        console.log(selectedGozareshId);
        console.log(currentSelectOptions);

        if (!selectedGozareshId || currentSelectOptions.length === 0) {
            alert('ابتدا یک گزارش انتخاب کنید.');
            return;
        }

        let row = `
<div class="row setting-row mb-2">
    <div class="col-md-3">
        <select class="form-select radif-select">
            <option hidden>ردیف</option>
            ${currentSelectOptions.map(o => `<option value="${o.full}" data-table="${o.table}">${o.full}</option>`).join('')}
        </select>
    </div>
    <div class="col-md-3">
        <select class="form-select soton-select">
            <option hidden>ستون</option>
            ${currentSelectOptions.map(o => `<option value="${o.full}" data-table="${o.table}">${o.full}</option>`).join('')}
        </select>
    </div>
    <div class="col-md-3">
        <select class="form-select value-select">
            <option hidden>مقدار</option>
            ${currentSelectOptions.map(o => `<option value="${o.full}" data-table="${o.table}">${o.full}</option>`).join('')}
        </select>
    </div>
    <div class="col-md-3">
        <button class="btn btn-danger remove-row">❌</button>
    </div>
</div>`;

        $('#selectorsContainer').append(row);
    });

    // حذف سطر
    $(document).on('click', '.remove-row', function () {
        $(this).closest('.setting-row').remove();
    });

    // ذخیره تنظیمات
// ذخیره تنظیمات
$('#saveConfigs').on('click', function () {
    let data = [];

    $('.setting-row').each(function () {
        // گرفتن ستون‌ها از سلکت‌ها
        let radifFull = $(this).find('.radif-select').val();
        let sotonFull = $(this).find('.soton-select').val();
        let valueFull = $(this).find('.value-select').val();

        // استخراج فقط نام ستون نهایی
        let radif = radifFull.split('.').pop();
        let soton = sotonFull.split('.').pop();
        let value = valueFull.split('.').pop();

        let radifTable = $(this).find('.radif-select option:selected').data('table');
        let sotonTable = $(this).find('.soton-select option:selected').data('table');
        let valueTable = $(this).find('.value-select option:selected').data('table');
        console.log(radifTable);
        console.log(valueTable);
        console.log(valueFull);
        console.log(sotonFull);
        console.log(radifFull);
        console.log(value);
        console.log(soton);
        console.log(radif);
        if (radif && soton && value) {
            data.push({
                FK_GOZARSH1: selectedGozareshId,
                RADIF: radif,
                TABLENAEM_RADIF: radifTable,
                SOTON: soton,
                SOTON_NAME: sotonTable,
                Value: value,
                Value_NAME: valueTable
            });
        }
    });

    if (data.length === 0) {
        alert("هیچ داده‌ای برای ذخیره وجود ندارد.");
        return;
    }
    console.log(data);

    $.ajax({
        type: 'POST',
        url: '@Url.Action("SaveGozaresh3", "Formula", new { area = "Salaries" })',
        contentType: 'application/json',
        data: JSON.stringify(data),
        success: function (res) {
            alert(res);
            $('#selectorsContainer').empty();
        }
    });
});
    </script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <link href="~/MyStyles/MyStyle.css" rel="stylesheet" />
    <script src="~/Scripts/jquery-3.3.1.js"></script>

</body>
</html>
