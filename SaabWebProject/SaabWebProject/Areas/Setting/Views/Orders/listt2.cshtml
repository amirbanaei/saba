@{
    Layout = null;
}
@model IEnumerable<SaabWebProject.Models.DomainModels.tbdetaol>
<link href="~/MyStyles/Tablestyle.css" rel="stylesheet" />
<div class="card">
    <div class="table-content p-4">
        <div class="tab-pane fade show active" id="allTab">
            <div class="table-responsive">
                <table class="table table-hover text-nowrap">
                    <thead>
                        <tr style="text-align:center">
                            @*<th>
            @Html.DisplayName("فعالیت")
        </th>*@
                            <th>
                                @Html.DisplayName("#")
                            </th>
                            <th>
                                @Html.DisplayName("کاربرحقوقی")
                            </th>
                            <th>
                                @Html.DisplayName("صورت وضعیت ")
                            </th>
                            <th>
                                @Html.DisplayName("  مبلغ کل ماشین")
                            </th>



                            <th>
                                @Html.DisplayName(" مبلغ ابزار ")
                            </th>





                        </tr>



                    </thead>
                    <tbody style="text-align:center">

                        @if (Model != null)
                        {

                            var i = 0;
                            foreach (var item in Model)
                            {
                                if (item != null)
                                {


                                    i++;


                                    <tr>
                                        <!--<td>-->
                                        @*<div class="ms-sm-4 mt-sm-0 mt-2 dropdown-toggle">
            <a href="#" data-bs-toggle="dropdown" class="text-white text-opacity-75 text-decoration-none">
                <i class="fa fa-edit"></i>
            </a>
            <div class="dropdown-menu">
                @if (item.For_Fish != true)
                {
                    <a data-bs-toggle="modal" data-bs-target="#modalLg1" class="dropdown-item edit" id="Edit_@item.installment_ID">ویرایش</a>
                    <a class="dropdown-item Delete" id="Delete_@item.installment_ID">حذف</a>

                }
                <a data-bs-toggle="modal" data-bs-target="#modalLg1" class="dropdown-item Details" id="Detail_@item.installment_ID">جزئیات</a>


            </div>
        </div>*@
                                        <!--</td>-->
                                        <td>
                                            @i
                                        </td>

                                        <td>
                                            @item.tbPeymanContracts.pec_Title

                                        </td>





                                        <td>
                                            @item.number_sorat

                                        </td>


                                        <td>
                                            @string.Format("{0:N0}", item.mashinkol)

                                        </td>
                                        <td>
                                            @string.Format("{0:N0}", item.abzarkol)

                                        </td>




                                    </tr>
                                }

                            }

                            // Your foreach loop code here
                        }

                    </tbody>

                </table>
            </div>
        </div>
    </div>
    <div class="card-arrow">
        <div class="card-arrow-top-left"></div>
        <div class="card-arrow-top-right"></div>
        <div class="card-arrow-bottom-left"></div>
        <div class="card-arrow-bottom-right"></div>
    </div>
</div>
<script>
    $(".edit").click(function () {
        var idd = $(this).attr("id").split("_")[1];
        console.log(idd);
        $("#big_modal_body1").load('@Url.Content("/installments/installments_Edit")?id='+idd);
    });
       $(".Details").click(function () {
        var idd = $(this).attr("id").split("_")[1];
        $("#big_modal_body1").load('@Url.Content("/installments/installments_details")?id='+idd);
    });

         @*$(".Delete").click(function () {
             var idd = $(this).attr("id").split("_")[1];
              console.log(idd);
             // $("#big_modal_body").load('@Url.Content("/installments/installmentsDelete")')?ReffrenceSaveID='+idd);
             ajax('@Url.Content("/installments/installmentsDelete")');
    });*@

</script>

<script>
          @*$(".Delete").click(function () {
              var idd = $(this).attr("id").split("_")[1];
                console.log(idd);
          $("#loading-message").css("display", "block");
          $.ajax({
              url: '@Url.Action("installmentsDelete", "installments", new { area = "ManagementAccounting" })',
              data: { ID: idd },
              type: "Post",
              dataType: "html",
              success: function (resp) {
                  $("#loading-message").css("display", "none");
                  if (resp == "True") {
                      Toast.fire({
                          icon: 'success',
                          background: '#35434A',
                          color: 'white',
                          title: "با موفقیت حذف شد"
                      }).then(() => {
                          location.reload();
                      });
                  } else {
                      Toast.fire({
                          icon: 'error',
                          background: '#35434A',
                          color: 'white',
                          title: "قادر به حذف  نیستید"
                      }).then(() => {
                      });
                  }

              },
              error: function (resp2) {
                  $("#loading-message").css("display", "none");
                  Toast.fire({
                      icon: 'error',
                      background: '#35434A',
                      color: 'white',
                      title: "حذف  با خطا مواجه شده است "
                  }).then(() => {
                      location.reload();
                  });
              }
          });
      });*@

          $(".Delete").click(function () {
              var idd = $(this).attr("id").split("_")[1];
                              console.log(idd);

    /*document.getElementById("loading-message").style.display = "block";*/





    Swal.fire({
        title: 'آیا از حدف قسط مطمئن هستید؟',
        icon: 'question',
        showCancelButton: true,
        background: 'rgb(29,40,52)',
        color: 'white',
        confirmButtonColor: '#3085d6',
        cancelButtonColor: '#d33',
        confirmButtonText: 'بله ',
        cancelButtonText: 'خیر'
    }).then((result) => {

        if (result.isConfirmed == true) {
            $(".loader").css('display', 'block');

          $.ajax({
              url: '@Url.Action("installmentsDelete", "installments", new { area = "ManagementAccounting" })',
              data: { ID: idd },
              type: "Post",
              dataType: "html",
              success: function (resp) {
                  $("#loading-message").css("display", "none");
                  if (resp == "True") {
                      Toast.fire({
                          icon: 'success',
                          background: '#35434A',
                          color: 'white',
                          title: "با موفقیت حذف شد"
                      }).then(() => {
                          location.reload();
                      });
                  } else {
                      Toast.fire({
                          icon: 'success',
                          background: '#35434A',
                          color: 'white',
                          title: "قادر به حذف  نیستید"
                      }).then(() => {
                      });
                  }

              },
              error: function (resp2) {
                  $("#loading-message").css("display", "none");
                  Toast.fire({
                      icon: 'success',
                      background: '#35434A',
                      color: 'white',
                      title: "حذف  با خطا مواجه شده است "
                  }).then(() => {
                      location.reload();
                  });
              }
          });
        }

    });






      });

    var Toast = Swal.mixin({
        toast: true,
        showConfirmButton: false,
        timer: 3000,
        timerProgressBar: true,
        didOpen: (toast) => {
            toast.addEventListener('mouseenter', Swal.stopTimer)
            toast.addEventListener('mouseleave', Swal.resumeTimer)
        }
    });
</script>