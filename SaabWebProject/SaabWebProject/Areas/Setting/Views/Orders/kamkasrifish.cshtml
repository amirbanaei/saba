
@{
    ViewBag.Title = "ثبت اکسل متن برای فیش";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<link href="~/Assets/plugins/select-picker/dist/picker.min.css" rel="stylesheet" />
<link href="~/Assets/plugins/BootstrapPersianDateTimePicker/dist/jquery.md.bootstrap.datetimepicker.style.css" rel="stylesheet" />

<script src="~/Assets/js/jalalidatepicker.min.js"></script>
<link href="~/Assets/css/jalalidatepicker.min.css" rel="stylesheet" />
<script src="~/Assets/js/jalaali.min.js"></script>
<link href="~/MyStyles/jalalidatepicker.css" rel="stylesheet" />

<link href="~/MyStyles/MyStyle.css" rel="stylesheet" />
<link href="~/MyStyles/Tablestyle.css" rel="stylesheet" />

<link href="~/MyStyles/inputstyleandformstyle.css" rel="stylesheet" />
@*برای input ih*@
<link href="~/MyStyles/cardstyle.css" rel="stylesheet" />
@*برای کارد  ها*@
<div class="tab-content ">
    <div class="mt-10px" id="loadd">
        <!--<div class="row">
            <div class="col-xl-10"></div>-->
        @*<div class="col-xl-2">
                <button class="btn btn-theme cssss form-control" style="border-radius:20px;color: white;background-color: #0d3244" data-bs-toggle="modal" data-bs-target="#modalLg" id="Detail">
                    ثبت جزئیات
                </button>
            </div>*@
        <!--</div>

        <div class="row">

            <div class="col-xl-5">
                <label class="form-label"> پیمان  </label>
                <div id="pymn10">

                </div>
            </div>
            <div class="col-xl-3">
                <label class="form-label"> کاربر </label>
                <span id="users"></span>
            </div>
            <div class="col-xl-3" id="Creditlamdicatros2">
                <label class="form-label"> مرکز هزینه   </label>
                <select class="form-select" id="creditline" style="display:none">
                    <option value="Select">انتخاب کنید</option>
                    @Html.Action("_ListAllCreditIndicatior", "CreditLine", new { area = "Budget" })
                </select>
                <select class="form-select" id="tbDetailedtitle" style="display:none">
                    <option value="Select">انتخاب کنید</option>
                    @Html.Action("_ListAlltbDetailedtitle", "FinancialDocuments", new { area = "ManagementAccounting" })
                </select>
            </div>
            <div class="col-xl-1">
                <a href="#" id="btnplus" class="btn buttonGeneralPlus form-control" data-bs-toggle="modal" data-bs-target="#modalLg">
                    <i class="fa fa-plus-circle fa-fw me-1 VecGeneral"></i>
                </a>
            </div>
            <div class="col-xl-2">
                <label class="form-label"> تاریخ </label>
                <div class="col-xl-12">-->
        @*------------------------------------------------[1402/10/14]-|KH|-*@
        <!--<input data-jdp jdp-btn-close data-jdp-miladi-input="datepickerFromEn_Eq1" id="datepickerFrom_Eq1" class="form-control">
        <input id="datepickerFromEn_Eq1" type="hidden" />-->
        @*-------------------------------------------------------------|KH|-*@
        <!--</div>
            </div>

            <div class="col-xl-2">
                <label class="form-label"> بستانکار (ریال) </label>
                <input class="form-control" id="pestenka" oninput="formatCurrency(this)" />
            </div>
            <div class="col-xl-2">
                <label class="form-label"> بدهکار(ریال) </label>
                <input class="form-control" id="bdehkar" oninput="formatCurrency(this)" />
            </div>
            <div class="col-xl-2">
                <label class="form-label">شماره </label>
                <input class="form-control" id="number" />
            </div>
            <div class="col-xl-4">
                <label class="form-label">  شرح  </label>
                <input class="form-control" id="Title" />
            </div>
        </div>-->
        <div class="row" style="margin-bottom:10px">
            <div class="col-xl-8"></div>
            <div class="col-xl-1">
                <a href="@Url.Action("ExportAddMoalefeExcel2del","Orders",new {area="Setting" })" class="btn form-control buttonGeneral">
                    <i class="bi bi-download fa-fw me-1 VecGeneral"></i>
                    <span class="bGeneral-text">دانلود فایل نمونه دسته ای کاربر</span>
                </a>
            </div>
            <div class="col-xl-1">
                <a class="btn form-control buttonGeneral" id="upload10" style="font-size:10px;margin-bottom:10px;">
                    <i class="bi bi-upload fa-fw me-1 VecGeneral"></i>
                    <span class="bGeneral-text"> بارگزای اکسل دسته ای کاربر</span>
                </a>
            </div>

            <input id="summery_file10" style="display:none" type="file" class="form-control" value="" />
            <input id="summery_file11" style="display:none" type="file" class="form-control" value="" />
            <input id="summery_file112" style="display:none" type="file" class="form-control" value="" />

            @*<div class="col-xl-1">
                    <button class="btn form-control buttonGeneral uploadSummery">
                        <i class="bi bi-upload fa-fw me-1 VecGeneral"></i>
                        <span class="bGeneral-text">بارگذاری پیوست</span>
                    </button>
                    <input id="summery_file" style="display: none;" type="file" class="form-control" value="" />
                </div>
                <div class="col-xl-1">
                    <button class="btn buttonsabt form-control" id="btnCreate1">
                        <i class="bi bi-clipboard-check fa-fw me-1 VecGeneral"></i>
                        <span class="bSabt-text">ثبت</span>
                    </button>
                </div>*@
        </div>
        <div class="row">
            <div id="myTable">

            </div>
        </div>
        <!-- arrow -->
    </div>
</div>


@section Scripts{
    <script src="~/Assets/plugins/select-picker/dist/picker.min.js"></script>
    <script src="~/Scripts/jquery-ui.js"></script>
    <script src="~/Assets/plugins/BootstrapPersianDateTimePicker/dist/jquery.md.bootstrap.datetimepicker.js"></script>

    @*------------------------------------------------[1402/10/14]-|KH|-*@
    <script>
        jalaliDatepicker.startWatch({
            time: false,
            days: ["شنبه", "1شنبه", "2شنبه", "3شنبه", "4شنبه", "5شنبه", "جمعه"],
            persianDigits: true,
            autoClose: true,
            //autocomplete: true,
            showCloseBtn: true,
            showEmptyBtn: false,
            showCloseBtn: true,
            zIndex: 100000,
        });
        //-----------------------------------------------------------------
        var inputs = document.querySelectorAll("[data-jdp-miladi-input]");
        for (var i = 0; i < inputs.length; i++) {
            inputs[i].addEventListener("jdp:change", function (e) {
                var miladiInput = document.getElementById(this.getAttribute("data-jdp-miladi-input"));
                var date = this.value.replace(' ', '').slice(0, 10).split('/');
                date = jalaali.toGregorian(parseInt(date[0]), parseInt(date[1]), parseInt(date[2]),)
                // If it was empty
                if (!this.value) {
                    date = "";
                }
                // If it was only history
                else if (this.value.replace(' ', '').length < 11) {
                    date = date.gy + '-' + date.gm + '-' + date.gd
                }
                // If it was the date and time
                else {
                    date = date.gy + '-' + date.gm + '-' + date.gd + ' ' + this.value.replace(' ', '').slice(10, 30)
                }
                miladiInput.value = date
            });
        }
    </script>
    @*-------------------------------------------------------------|KH|-*@
    <script>
        function formatCurrency(input) {
            // حذف همه کاراکترهای غیر از اعداد و "." (جداکننده اعشاری)
            let value = input.value.replace(/[^\d.]/g, '');

            // تبدیل مقدار به عدد اعشاری
            let number = parseFloat(value);

            // اگر عدد معتبر باشد، آن را به صورت سه رقمی جداکنده کنید
            if (!isNaN(number)) {
                let formattedValue = number.toLocaleString('en-US', { minimumFractionDigits: 0, maximumFractionDigits: 0 });
                // قرار دادن مقدار فرمت شده در ورودی
                input.value = formattedValue;
            }
        }
    </script>
    <script>
        $("#users").load('@Url.Content("/Users/Users/_GetAllUsers_Select")',function(){
         $('#select_users').attr("id","select_users1");
                 var $elem2 = $('#select_users1');
            $elem2.picker();
                         $("#creditline").show();
                        $("#tbDetailedtitle").hide();
                 $elem2.on('sp-change', function () {
                      $("#creditline").show();
                        $("#tbDetailedtitle").hide();
                    var typeVal = $('#select_users1').val();
                    if (typeVal == 'Select') {
                        $("#creditline").show();
                        $("#tbDetailedtitle").hide();
                    } else {
                        $("#tbDetailedtitle").show();
                        $("#creditline").hide();
                    }
                });
        });
    </script>
    @*<script>
            $("#myTable").load('@Url.Content("/ManagementAccounting/FinancialDocuments/listdocum")');
        </script>*@
    <script>
        $("#btnplus").click(function () {
              $("#big_modal_body").load('@Url.Content("../../ManagementAccounting/FinancialDocuments/_CreatetbDetailedtitle")');
        });
           $("#btnplus2").click(function () {
         $("#big_modal_body1").load('@Url.Content("../../ManagementAccounting/FinancialDocuments/Batchpymn")');
           });
       $("#btnplus3").click(function () {
         $("#big_modal_body1").load('@Url.Content("../../ManagementAccounting/FinancialDocuments/Batchuser")');
   });
    </script>
    <script>
        var Toast = Swal.mixin({
            toast: true,
            showConfirmButton: false,
            timer: 3000,
            timerProgressBar: true,
            didOpen: (toast) => {
                toast.addEventListener('mouseenter', Swal.stopTimer)
                toast.addEventListener('mouseleave', Swal.resumeTimer)
            }
        });
        var fd = new FormData();
        function sendAjax2(url2, fd2) {
            var debtorCreditValue = $("#pestenka").val();
            var resultString = debtorCreditValue.replace(/,/g, '');
            fd2.append("Creditor", resultString);
            var debtorCreditValue2 = $("#bdehkar").val();



            console.log(resultString)
            var resultString2 = debtorCreditValue2.replace(/,/g, '');
            console.log(resultString2)
            console.log(resultString2)

            console.log(resultString)

            fd2.append("Title", $("#Title").val());
            fd2.append("Debtore", resultString2);
            fd2.append("Peyman_ID", $("#select_peyman4").val());
            fd2.append("Peyman_ID", $("#select_peyman4").val());
            fd2.append("numberpayvas", $("#number").val());

            var mod = $("#select_users1").val()
            if (mod == 'Select') {
                fd2.append("User_ID", null);
            }
            else {
                fd2.append("User_ID", mod);
            }
            var mod1 = $("#creditline").val()
            if (mod1 == 'Select') {
                fd2.append("Creditlamdicatros_ID", null);
            }
            else {
                fd2.append("Creditlamdicatros_ID", mod1);
            }
            var mod2 = $("#tbDetailedtitle").val()
            if (mod2 == 'Select') {
                fd2.append("Detailedtitle_ID", null);
            }
            else {
                fd2.append("Detailedtitle_ID", mod2);
            }

            fd2.append("DataDocument", $("#datepickerFromEn_Eq1").val());
            fd2.append("Peyman_ID", $("#select_peyman4").val());
            console.log($("#datepickerFromEn_Eq1").val());

            $.ajax({
                url: url2,
                type: 'POST',
                data: fd2,
                processData: false,
                contentType: false,
                success: function (resp1) {
                    console.log("okeye");
                    console.log(resp1);
                    $("#loading-message").css("display", "none");
                    fd = new FormData();
                    if (resp1 == "True") {
                        alert("با موفقیت ثبت شد");
                        location.reload();
                    }
                    else {
                        $("#loading-message").css("display", "none");
                        Toast.fire({
                            icon: 'success',
                            background: '#35434A',
                            color: 'white',
                            title: "با خطا مواجه شده است ، لطفا بعدا تلاش فرمایید"
                        }).then(() => {
                            $("#loading-message").css("display", "none");
                        });
                    }

                },
                error: function (resp1) {
                    console.log(resp1);
                    console.log("Nokeye");
                    $("#loading-message").css("display", "none");
                    Toast.fire({
                        icon: 'success',
                        background: '#35434A',
                        color: 'white',
                        title: "با خطا مواجه شده است ، لطفا بعدا تلاش فرمایید"
                    }).then(() => {
                        $("#loading-message").css("display", "none");
                    });
                }
            });
        }
    </script>
    <script>
           $("#pymn10").load('@Url.Content("/../Contracts/Peyman/_GetAllListPeymans_Select")',function(){
                $("#select_peyman").attr("id", "select_peyman4");
                $("#select_peyman4").picker({ search: true });
           });
           @*$("#user1").load('@Url.Content("/../Users/Users/_GetAllUsers_Select")',function(){
                $("#select_users").attr("id", "select_users40");
                $("#select_users40").picker({ search: true });
           });*@
    </script>
    <script>
      $("#Detail").click(function () {
        $("#big_modal_body").load('@Url.Content("../../ManagementAccounting/FinancialDocuments/_Submit_Detail")');
      });

      $("#peyman1").load('@Url.Content("/../Contracts/Peyman/_GetAllListPeymans_Select")',function(){
        $("#select_peyman").attr("id", "select_peyman1");
        var $elem = $('#select_peyman1');
        $elem.picker();
        $elem.on('sp-change', function () {});
        $("#peyman2").load('@Url.Content("/../Contracts/Peyman/_GetAllListPeymans_Select")',function(){
           $("#select_peyman").attr("id", "select_peyman2");
            var $elem2 = $('#select_peyman2');
            $elem2.picker();
            $elem2.on('sp-change', function () {});
       });
    });
    </script>
    <script>
        $(".uploadSummery").click(function () {
            $("#summery_file").click();
        });

        $("#summery_file").change(function (e) {
            $.each(e.target.files, function (index, file) {
                fd.append("files", file);
            });
        });
    </script>
    <script>
        $("#btnCreate1").click(function () {
            if ($("#creditline").val() == 'Select' && $("#tbDetailedtitle").val() == 'Select') {
                alert("لطفا مرکز هزینه را انتخاب نمائید");
            }
            else if (($("#Title").val() == 0))
                 {
                alert("لطفا شرح را وارد  نمائید");
            }
           else if (($("#select_peyman4").val() == null))
           {
             alert("لطفا پیمان را انتخاب نمائید");
           }
           else if (($("#datepickerFrom_Eq").val() == 0))
           {
             alert("لطفا تاریخ  را انتخاب نمائید");
           }
           else {
               console.log($("#datepickerFromEn_Eq1").val());
             sendAjax2('@Url.Action("UploadAttachmentFile", "FinancialDocuments",new { area = "ManagementAccounting" })', fd);
           };
    });
            $("#upload10").click(function () {





      $("#summery_file10").click();

        });
       fd3 = new FormData();

$("#summery_file10").change(function (e) {
             $.each(e.target.files, function (index, file) {
                 fd3.append("MyExcelStream", file);
             });
    sendAjax14('@Url.Action("GetDataFromExcel_asnalpersonaldel", "Orders", new { area = "Setting" })', fd3);
});

        function sendAjax14(url2, fd2) {

            document.getElementById("loading-circle").style.display = "block";


    $.ajax({
        url: url2,
        type: 'POST',
        data: fd2,
        processData: false,
        contentType: false,
        success: function (res) {
            document.getElementById("loading-circle").style.display = "none";

            $("#loading-message").css("display", "none");
            fd = new FormData();
            console.log(res);
            console.log("jj");

            if (res == "True") {
                alert("با موفقیت ثبت شد ");
            //    location.reload();
            }
                   @*$("#table_div").load('@Url.Content("../defFacilitiesEquipment/_ListEquipmentMoalefeValue")');*@



        },
        error: function (res) {
            $("#loading-message").css("display", "none");
            console.log(res);
            console.log("jj");
            Toast.fire({
                icon: 'success',
                background: '#35434A',
                color: 'white',
                title: "با خطا مواجه شده است ، لطفا بعدا تلاش فرمایید"
            }).then(() => {
                $("#loading-message").css("display", "none");
            });
        }
    });
}

    </script>
}