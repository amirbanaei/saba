@model List<SaabWebProject.Models.DomainModels.tbjob>
@{
    Layout = null;
}
<select name="usr_typeshoghl" class="form-select" id="FK_job">
    <option value="0">انتخاب نمایید..</option>

    @foreach (var item in Model.ToList())
    {
        <option value="@item.ID">@item.Name</option>
    }
</select>

